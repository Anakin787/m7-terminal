# 작업일지

프로젝트 진행 기록. 최신 항목이 위에 옵니다.

---

## 2026-09-16 — 손으로 산 주식을 엔진에게서 숨기고, 화면이 말 안 하던 것 셋

### 9/15, 봇이 내 IONQ를 팔려고 했다

9/10 이후 토스 계좌로 **IONQ 13주**를 직접 샀다. 유니버스 안에 있는 종목이라
"봇이 덜 사겠거니" 정도로 보고 아무 장치도 넣지 않았는데, 그 판단은 **매수에만**
맞았다.

9/15(화) 23:35 실행, 세션 월요일 9/14. 그 주의 첫 리밸런스에서 나온 신호는 1건,
그런데 매수가 아니라 **매도**였다.

```
bucket_dca-IONQ-2026-09-14-1  side=SELL  quantity=13  notional_krw=666,302
>>> 신호 1건 · 승인 1건 · 거부 0건
```

`_rotation_exits`는 gap을 안 본다. 랭킹만 본다 — 점수가 `min_score` 아래로
떨어지고 자기 추세까지 깨지면 `exit_fraction=1`, 즉 **보유 전량**이다. 누가 샀는지는
구분하지 않는다. 계좌가 가진 수량에 대한 비율이지 전략이 산 수량에 대한 비율이
아니기 때문이다.

막을 수 있었던 것이 둘 있었는데 둘 다 안 막았다. **AI 보류가 걸려 있었다** — 로그에
`보류(AI): IONQ — 스카이워터 인수합병…`이 찍혔는데도 통과했다. veto는 설계상
**매수만** 막는다(`risk.py`: "a veto only ever blocks a *buy*… never a reason to dump
a position"). 그리고 **리스크 게이트가 승인했다** — 토스가 13주를 매도가능으로
보고했으니 거부할 이유가 없었다. PAPER라 `simulated`로 끝났을 뿐이다.

### `trading.excluded_holdings`

`{종목: 수량}`. 엔진이 "없는 셈" 쳐야 하는 보유다.

**빼는 위치가 전략이 아니라 `build_context`인 게 핵심이다.** 리스크 게이트가 같은
컨텍스트를 읽기 때문에, 한 곳에서 포지션을 줄이면 세 가지가 한꺼번에 따라온다 —
전략이 목표 계산에서 빼고, 매도를 제안하지 못하고, 게이트가 그 매도를 승인하지
못한다.

`sellable`도 같이 줄였다. 포지션만 숨기면 "제안"을 막는 것이고, 브로커
매도가능수량까지 줄여야 혹시 제안이 나와도 게이트가 걸러낸다. **두 곳이 동시에
실패해야** 손으로 산 주식이 움직인다.

**스냅샷은 그대로 둔다.** 눈이 먼 건 매매 엔진이지 장부가 아니다 — 리포트와
대시보드는 계속 13주를 보여준다. 이게 `portfolio.manual`(타 증권사, 리포트용)과
다른 점이다.

`Position.with_quantity`를 새로 만들면서 하나 걸렸다. `evaluation`이 `market_value`를
`수량 × 가격`보다 우선한다. 수량만 줄이면 **줄어든 수량에 원래 평가금액이 붙어서**,
전략이 목표를 계산하는 기준이 전부 틀어진다. 비율로 같이 스케일한다.

부분 제외도 된다 — 18주 보유 + 13주 제외면 전략은 5주만 보고, 로테이션 매도 때 그
5주만 판다.

수량 0 이하는 로드 시점에 거부한다. 오타는 조용히 실패하는 종류라 — 보호된다고
믿는 주식을 엔진이 다시 관리하기 시작하고, 첫 징후가 나서는 안 될 매도다.

**배포 후 검증:** 수동 실행 로그가 `보유 2종목`. 직전 실행은 3종목이었다.

### 대시보드에 세 가지를 채웠다

Holdings가 33줄짜리 표 하나로 제일 비어 있었다.

**종목 차트.** 보유 종목을 골라 일봉을 본다. **평균 매수가 점선이 이 차트의 존재
이유다** — 종목 차트야 어디서든 보지만 *이 계좌가 어디서 샀는지*는 여기서만 볼 수
있고, 종가가 그 선 위인지 아래인지가 보유 표가 정말로 묻고 있는 질문이다. 그래서
주석이 아니라 가격선과 대등한 선으로 그렸고, y축 범위에도 포함시켰다 — 크게 물린
종목이면 가격만 그려지고 정작 봐야 할 선이 박스 위로 잘려 나간다.

라이브러리는 안 썼다. `app.js` 맨 위가 "차트는 손으로 그린 SVG"라고 정해 뒀고,
필요한 폼이 이미 그리고 있는 그것이었다. `attachHover`를 일반화하는 걸로 끝났다.

**AI 보류 화면.** 엔진은 로그에 찍고 게이트는 거부하는데, 사람이 보는 화면 어디에도
"이 종목은 매수가 멈춰 있다"는 말이 없었다. 근거와 만료일까지 보여주고, 해제는
킬 스위치와 똑같이 감사 로그에 남긴다 — 다음 실행부터 엔진이 살 수 있게 되는
변경이니까. `Store.vetoes()`는 기존 `active_vetoes()`를 안 건드리고 따로 뒀다.
게이트는 `{종목: 사유}`만 받아야 하고, 사람은 그 이상이 필요하다.

**벤치마크.** Overview가 `-39%`를 보여주는데 비교 대상이 없었다. 대시보드 코드
전체에 `benchmark`라는 단어가 한 번도 없었는데, 정작 봉은 캐시에 있고 스냅샷은
Firestore에 있었다.

**벤치마크에 같은 현금흐름을 넣는다.** 시작점만 맞추는 방식은 간단하고 돈이 들어오는
순간 거짓말이 된다 — 이 계좌는 9/11에 매입원가가 670,723원 올랐고(IONQ 매수), 그걸
안 받은 벤치마크였다면 **입금을 전략이 시장을 이긴 것처럼** 그렸을 것이다. 반대로
매입원가가 줄 때는 되팔지 않는다. 대개 계좌 안에서 갈아탄 것이지 돈이 나간 게
아니라서, 출금으로 치면 벤치마크에 계좌가 가진 적 없는 현금을 쥐여주게 된다.

현재 구간: **내 계좌 −10.63% vs QQQ +1.93%.**

### 만들다 나온 것: 로더가 꼬리만 채우고 있었다

`HistoryLoader.refresh`가 캐시를 마지막 마감 세션과 비교해서 최신이면 바로
반환했다. 캐시가 요청한 **시작일부터 있는지는 한 번도 안 봤다.**

그래서 시계열 길이가 **"누가 처음 요청한 창"에 영원히 고정**되고, 아무것도 그걸
알려주지 않는다. 대시보드의 전일종가 조회가 12일치를 요청하므로 TSLL·IONX는 1Y를
눌러도 21개뿐이었다. 양쪽 끝을 다 채우게 고쳤다 — 세 종목 모두 275개(1년치)가 됐다.
상장이 그보다 늦은 종목은 빈 응답을 받는 요청이 한 번 더 나가는데, 휴일 캘린더를 안
들고 다니는 대가로 이미 치르고 있는 것과 같은 종류다.

### 그리고 내가 넣은 버그 하나

Portfolio Value 차트에 마우스를 올려도 아무것도 안 나왔다. 종목 차트는 같은
`attachHover`를 쓰는데 멀쩡했다.

```js
const valueOf = options.valueOf || ((p) => p.total_krw);
```

`options`는 `{}`이고 **모든 객체는 `Object.prototype.valueOf`를 상속한다.** 왼쪽이
항상 함수라 기본값에 영영 도달하지 못한다. 옵션을 안 넘기는 포트폴리오 차트는
데이터 포인트에 대고 `Object.prototype.valueOf`를 호출해
`Cannot convert undefined or null to object`를 던졌고, 그게 **mousemove 핸들러
안**이라 화면엔 "아무 일도 안 일어남"으로만 보였다. 종목 차트는 그 옵션을 명시적으로
넘겨서 상속된 것을 건드린 적이 없다.

**여기서 두 번 헛짚었다.** 증거 없이 기하 쪽을 먼저 의심해서, viewBox가 낡는 문제와
좌표 변환을 고치고 두 번 "됐다"고 했는데 둘 다 원인이 아니었다. 결국 헤드리스 크롬을
띄워(시스템 라이브러리 3개를 root 없이 받아서 `LD_LIBRARY_PATH`로 물렸다) 호버를
시뮬레이션하고 나서야 스택이 나왔다 — `at valueOf (<anonymous>)`. **브라우저 버그는
브라우저로 보고 나서 고쳤어야 했다.**

그 두 커밋은 그 자체로는 맞는 수정이라 남겼다. Allocation 탭을 By Plan으로 바꾸면
범례가 1행에서 4행이 되고 그리드가 옆 차트를 늘리는데, 그때 viewBox가 낡는 건 실제
문제였다(이제 `ResizeObserver`가 박스를 본다). 좌표도 비율 나눗셈보다
`getScreenCTM` 행렬이 옳다 — 레터박싱·확대·transform을 다 반영한다.

### 검증

- **테스트 554건** (세션 시작 시 480건)
- **실브라우저 확인** — 두 차트 모두 세로·가로 격자선·점·툴팁 표시, 페이지 오류 없음
- **탭 높이** — By Market / By Currency / By Plan 모두 354px

---

## 2026-09-10 — 첫 신호가 나오자마자 드러난 것들

### 9/9, 처음으로 신호와 주문이 나왔다

`m7-trade` 23:35 KST 실행, 세션 월요일 9/8. **신호 8건 · 승인 8건 · 거부 0건.**
SHY·GLD 각 $76.47, AMD·INTC·JPM·JNJ·UNH·V 각 $50.98 — 합계 **$458.82 / 예산
$484.31**. `_budget` → 게이트 → `OrderExecutor` 경로가 처음으로 끝까지 돌았다.

수요일에 나온 것도 정상이다. 9/7 월요일이 미국 노동절 휴장이라 make-up 규칙(앵커
요일이 휴장이면 그 이후 **첫** 세션)이 9/8 화요일을 매수일로 잡았고, 그 세션이 마감된
뒤 실행에서 발동했다. **그 규칙이 실전에서 처음 값을 한 순간이다** — 없었으면 그 주는
조용히 건너뛰었다.

### 그 8건이 가리고 있던 것 둘

둘 다 같은 이야기다. **안전장치가 한 번도 부하를 받은 적이 없었다.**

**① 일일 한도가 자기 실행의 주문을 못 봤다.** `ctx`는 루프 전에 한 번 읽는
스냅샷인데 신호 8건이 전부 그 하나로 평가돼서, 각자 "나머지 7건이 없는 하루"를
기준으로 통과했다. `max_orders_per_day: 10`은 11번째를 못 막는다 — 11번째도 0과
비교하니까. 어제 안 터진 건 8 < 10, 615,209 < 30,000,000이었기 때문일 뿐이고,
**PAPER로는 영원히 안 드러난다** — 한도는 넘으려는 배치만이 시험한다.

`BatchCommitment`(주문 수·원화 금액·주문통화 매수여력·종목별 편입금액·매도 약정
수량의 델타) + `RiskGate.evaluate_batch`. 승인분만 누적한다 —
`Store.daily_usage`가 rejected 행을 빼는 것과 같은 규칙이다. 백테스트 엔진도 같은
one-ctx 루프였어서 같이 배치화했다. 안 하면 백테스트와 라이브가 한도에 대해 서로
다른 말을 한다.

**② 멱등키가 KST 자정에 갈렸다.** `client_order_id`의 날짜와 `daily_usage`의 조회
창이 둘 다 벽시계 날짜인데, 이 잡은 **23:35 KST**에 뜨고 `timeoutSeconds`가
**1800(30분)** 이다 — 자정까지 남은 25분보다 길다. 1차가 매달려 00:05에 재시도되면
`session_date`는 9/8 그대로라 같은 8건이 다시 나오고, 멱등키·주문수·금액 한도가
**셋 다 동시에** 리셋된다.

```
1차 23:35 → bucket_dca-SHY-2026-09-09-1  duplicate: False
2차 23:52 → bucket_dca-SHY-2026-09-09-1  duplicate: True
3차 00:05 → bucket_dca-SHY-2026-09-10-1  duplicate: False   ← 새 주문
```

주문 행에 `session_date`를 저장하고 양쪽을 그 값으로 키잉했다. `session_date`는
**캘린더가 아니라 봉에서** 읽는다 — 전략이 `today`로 쓰는 바로 그 값이라 둘이
어긋날 수 없다. 심볼 간 max라 피드 하나가 조용해도 하루 밀리지 않고, history가
없으면 벽시계로 폴백해서 백테스트 포함 기존 호출자는 동작이 그대로다.

`trade.py`가 이제 세션 날짜를 로그에 찍는다. 이 프로젝트가 값을 치른 침묵이 전부
이 값 하나였다 — 8일 무실행도, 주 3회 리밸런스도.

### LIVE 전용 경로를 읽어서 둘 더

PAPER는 `_finish(STATUS_SIMULATED)`로 빠져나가서 `_send`·`_handle_error`·reconciler를
통째로 건너뛴다. 아직 아무도 시험한 적 없는 구간이라 [10] 전에 읽었다.

**③ 금액 주문은 절대 `filled`이 될 수 없었다.** `_infer_status`가 체결수량 vs
주문수량 비교로 완료를 판정하는데, 금액 주문("$50.98어치")은 주문수량이 `None`이라
항상 `partially_filled`로 떨어진다. **bucket-dca 매수는 전부 금액 주문**이라 활성
전략의 모든 주문이 해당 — 대시보드에 영원히 미체결로 남고, `partially_filled`가
`_OPEN_ORDER_STATUSES`에 있어 30분마다 무한 재조회된다. 잔량 필드 → 상태 문자열 →
partially 순으로 사다리를 놨다. **부정 힌트를 먼저 본다** — `"unfilled"`는
`"filled"`를 포함하고 `"미체결"`은 `"체결"`을 포함해서, 반대로 하면 전부 완료로
읽힌다.

**④ 응답 못 받은 발주가 `failed`로 기록됐다.** `network-error`와 5xx가 어느 분기에도
안 걸려 맨 아래 `failed`로 갔는데, `failed`는 종결 상태라 아무도 다시 안 본다.
그런데 타임아웃된 POST는 **주문이 들어갔을 수도 있는** 가장 애매한 경우다.
`IN_FLIGHT_CODES`처럼 `unknown`으로 보내 reconciler가 clientOrderId로 정산하게 했다.
4xx는 브로커가 판단한 것이므로 `failed` 유지. 곁들여 `_fetch`가 "못 물어봄"과
"물어봤는데 없음"을 구분한다 — 전에는 둘 다 "조회 실패"라 이력에 없는 주문이 영원히
재시도됐다.

### `scripts/live_check.py`

설계 [10]이 요구하는 "**최소 수량 1주**로 실거래 검증"을 할 도구가 아예 없었다.
`trade.py`는 전략이 낸 신호를 통째로 처리하는 것뿐이고, bucket-dca는 한 번에 8건
수백 달러를 낸다. 첫 실거래로 삼을 수 없다.

우회로가 아니다 — 같은 `build_context`, 같은 `RiskGate`, 같은 `OrderExecutor`.
API를 직접 부르는 스크립트였다면 토스가 동작한다는 것만 증명하고 이 프로젝트가
실제로 쓴 코드는 전부 미검증으로 남는다.

`--amount`도 받는다. bucket-dca 매수는 전부 금액 주문이고 reconciler가 수량 주문과
**다른 분기**에서 정산한다 — 위 ③ 그 분기다. 1주는 경로를, 소액 금액 주문은 실제
주문이 탈 분기를 증명한다.

안전장치는 거부하는 방식에 있다. `--execute` 없이는 무전송, 보낼 땐 **종목코드를
그대로 타이핑**(`y` 불가), 비대화형이면 `--yes` 없이 거부. 어떤 Cloud Run 잡의
인자에도 없고, `trade.py --live`는 여전히 거부 상태 그대로다.

만들다 하나 잡았다. 미리보기가 ID를 f-string으로 조립했는데
`make_client_order_id`는 안전문자셋 밖을 `_`로 바꾼다 — `live-check` →
`live_check`. 실제와 다른 ID를 보여줄 뻔했다.

---

## 2026-09-08 — 대시보드: 없던 화면 하나와, 화면이 스스로 말하게 한 것들

실거래 점검을 끝내고 대시보드를 훑으면서 나온 다섯 건. 하나는 없던 기능이고
넷은 화면이 스스로 못 하던 말을 대신 문장으로 때우고 있던 자리다.

### 신호·주문 화면이 없었다

Trading 페이지의 "신호·주문 이력 화면은 아직 없습니다"는 **손으로 써둔 고정
문단**이었다. 주문이 백 건 나가도 저 문장 그대로다.

그런데 **어려운 쪽은 이미 다 돼 있었다.** `[8]` 이후로 `save_decision`이 승인·거부
신호를 똑같이 적고, `save_order`가 요청을 보내기 *전에* 행을 남기고,
`recent_signals()`는 심지어 거부 규칙을 `reject_rule`/`reject_detail`로 펼쳐서
돌려준다. 없던 건 저장소 위의 두 겹뿐이었다 — 엔드포인트 하나와 렌더 함수 하나.

**표를 둘로 나눴다.** 게이트가 거부한 신호는 주문이 되지 않는다. 하나로 합치면 그
행을 버리거나 빈 주문을 지어내야 하는데, **"게이트가 전부 막았다"가 이 화면을 여는
이유**다. 주문 행마다 모드를 붙인 것도 같은 이유다 — 설계 7절이 PAPER/LIVE 혼동을
실거래 리스크로 꼽고, 주문 표가 그게 물리는 자리다.

**폴링은 안 걸었다.** 엔진 상태를 나르는 15초 새로고침에 얹으려다 계산해 보니,
Firestore 문서 100개를 15초마다 읽으면 **놀고 있는 한 시간에 무료 할당량의 절반**을
쓴다. bars.db를 Firestore에서 뺀 것과 같은 산수다. 페이지를 열 때만 읽는다.

**빈 상태를 세 갈래로 갈랐다.** 그냥 비워두면 "아직 안 돌았다"와 "돌았는데 전부
거부됐다"가 똑같이 보인다. 이 프로젝트가 계속 값을 치른 그 침묵이라, 신호는 있고
주문만 없을 때는 "위 표의 판정을 보세요"라고 말한다.

### Engine Control을 접었다

세 타일 + 입력창 + 정지 버튼이 하루에 한 번 볼까 말까 한 패널치고 화면을 너무 많이
먹고 있었다. 특히 그 아래에 신호·주문 표가 생기고 나서는 **그날의 실제 내용을
접히는 자리 밑으로 밀어냈다.**

접어도 되는 이유는 **헤더의 PAUSE 버튼이 모든 페이지에 있어서**다. 이 카드가 유일한
정지 수단이었던 적이 없다.

안전선 둘을 걸었다. 상태 칩(`ARMED · PAPER` / `HALTED`)은 토글 **밖**에 남겼고 —
접히는 건 *왜 그런지*지 *무엇인지*가 아니다 — 정상이 아니면(중단·비활성·상태 못 읽음)
**강제로 펼쳐진다.** 그 이유가 안에 있는 세 타일 중 하나라, 안 눌러본 사람은 빈 표만
보고 설명은 못 보게 되기 때문이다.

### 감사 로그·리포트에 페이지네이션

둘 다 "최신 20건" 또는 "최신 50건"이었고, 그보다 오래된 건 그냥 없었다.

**두 표가 서로 다르게 페이징한다.** 쿼리가 다르기 때문이다. 리포트는 Firestore
`offset`을 그대로 쓴다 — 건너뛴 문서만큼 읽기 비용을 물지만 리포트는 하루 한 건이라
1년이 열두 페이지고, **정직한 페이지 번호**가 커서가 아껴줄 읽기보다 낫다.

**감사 로그는 그럴 수 없다.** 카테고리 필터가 Python에서 걸린다(`where`를 다른 필드의
`order_by` 옆에 붙이면 배포마다 복합 인덱스가 필요한데, **에뮬레이터는 그걸 그냥
받아줘서 운영에서 처음 터진다** — 원래 주석에 적혀 있던 이유다). Firestore offset을
쓰면 다른 카테고리 행을 건너뛰고 엉뚱한 페이지가 나온다. 스캔 → 필터 → 슬라이스로
바꿨고, 총계는 같은 패스에서 나온다. veto/limits를 번갈아 넣은 테스트로 고정했다 —
offset이 먼저 걸리면 우연히 정답이 나올 수 없게.

`_AUDIT_SCAN = 500` 상한은 `500+`로 표시하고 툴팁으로 말한다. **이름을 못 대는
페이지에서 조용히 멈추는 페이저**는 또 하나의 침묵이다.

렌더러가 처음에 `51–50 / 47건`과 `3 / 2`를 뱉었다. 산수지 위치가 아니라서, 끝을
넘어간 페이지는 "이 페이지는 비어 있습니다"라고 말하고 **이전 버튼을 살려둔다.**

**덤으로 막은 것:** `loadReports()`가 Overview의 AI 카드도 같이 채우고 있었다. 그대로
뒀으면 리포트 3페이지로 넘기는 순간 "최신 인사이트"가 3월 리포트로 조용히 바뀐다.
1페이지일 때만 갱신한다.

### 화면이 스스로 말하게 한 둘

**AI 코멘트의 `**`.** Overview 카드와 Reports 셀은 텍스트로 찍으므로 `**`가 굵게 되는
일이 없다. 그냥 오타처럼 앉아서 **260자 발췌 예산만 갉아먹고** 있었다. Gemini가
마크다운으로 답하는 건 Notion이 그걸 렌더링하기 때문이고, 그 사본은 그대로 뒀다.
벗기기를 *자르기 전에* 하도록 순서도 바꿨다 — 전에는 자른 뒤라 잘린 자리가 `**`
안이면 반쪽이 남았다.

**감사 로그 헤더 5줄 → 2줄.** 네 문장 중 셋이 화면에 이미 있는 걸 말로 다시 설명하고
있었다: 근거 줄은 화면에 그대로 보이고, 정확/근사치는 이미 툴팁에 있었다. 네 번째
"마우스를 올리면 설명이 나옵니다"는 **툴팁에 안내문이 필요하다는 건 표시가 없다는
뜻**이라, 문장 대신 점선 밑줄과 `cursor-help`를 줬다. 표가 스스로 말하지 못하는 건
하나뿐이다 — 시각이 왜 둘이냐.

곁들여 `max-w-3xl`도 뺐다. 문단에 적당한 읽기 폭을 주는 클래스인데 **이건 문장
하나**라, 1600px 화면에서 얻는 것 없이 두 줄로 만들고 있었다.

### 검증

이 환경에 브라우저가 없어서(8/28 그대로), **실제 렌더 함수를 스텁 DOM에 올려**
돌렸다. 신호·주문 표 세 경우, Engine Control 여섯 상태, 페이저 일곱 경계. 엔드포인트는
Firestore 에뮬레이터로 파이썬 테스트 6건 추가. 465 → 471건.

클라우드 배포까지 마쳤다(`m7-dashboard` 리비전 00002, 이미지 `2d676f2`).

---

## 2026-09-08 — 주 1회 리밸런스가 실은 주 3회였다

실거래 전환이 가능한지 물으셔서 점검하다 나왔다.

`HistoryLoader.refresh`는 캐시의 마지막 봉이 `end`(=오늘)로부터 3일 이내면
받아오는 걸 통째로 건너뛰었다. 백테스트 편의로는 말이 되는 규칙인데, **바
캐시를 갱신하는 곳은 `trade.py` 하나뿐**이다(`main.py`는 `HistoryLoader`를
쓰지 않는다). 그래서 건너뛴 실행은 캐시를 그대로 둔 채 끝나고, 전략은 그
마지막 봉의 날짜를 `today`로 앵커링한다(`bucket_dca.evaluate`).

평범한 한 주가 이렇게 흘렀다:

| KST 실행 | `end − last` | 갱신 | 전략이 읽는 `today` | 리밸런스 |
|---|---|---|---|---|
| 월 23:35 | 3 | 건너뜀 | 금 | — |
| 화 23:35 | 4 | 갱신 | **월** | **발동** |
| 수 23:35 | 2 | 건너뜀 | **월** | **발동** |
| 목 23:35 | 3 | 건너뜀 | **월** | **발동** |
| 금 23:35 | 4 | 갱신 | 목 | — |

**8/28 항목과 같은 실패를 다른 길로 다시 만났다.** 그때는 보충 매수 규칙이
"앵커 이후 모든 세션"이 되면 한 주에 서너 번 산다는 걸 알고 *첫* 세션으로
제한했는데, 이번엔 규칙이 아니라 **캐시가** `today`를 월요일에 붙들어 같은
결과를 냈다.

- **매도는 무사했다.** `_rotation_in_flight()`와 부모의 `_exit_in_flight()`가
  cooldown 3일로 재발주를 막는다.
- **매수에는 가드가 없다.** `_target_buys()`에 in-flight 검사가 없고, 중복
  주문 방지도 안 걸린다 — `client_order_id`의 `day`가 세션 날짜가 아니라
  **벽시계 날짜**(`executor.py:143`)라 화/수/목이 서로 다른 id가 된다.
- 지금 계좌(USD 509)에서는 실피해가 없다. 화요일에 현금을 다 쓰면 수·목엔
  `usd_power ≈ 0`이라 저절로 멈춘다. **문제는 계좌가 커져
  `max_deploy_per_week_pct = 0.20`이 실제로 바인딩할 때**다 — 주당 20%가
  아니라 60%가 나간다. A1에서 만든 통제가 조용히 무력화된다.

### 고친 것

갱신 여부를 **`end`가 아니라 거래소 기준**으로 판단한다. 캐시가 *마지막으로
마감된 세션*에 못 미치면 받아온다.

- `src/data/sessions.py`를 새로 만들어 `last_closed_session_date()`를 옮겼다.
  로더는 소스에 의존하지 않는 계층이고(`source`는 주입받는다), 달력 사실
  하나 때문에 구체적인 소스 모듈을 import하게 둘 수는 없었다. `yahoo.py`는
  이제 여기서 가져다 쓴다.
- `staleness_days`는 **오프라인 읽기 전용**으로 좁혔다 — 캐시 꼬리가 `end`에
  얼마나 뒤처지면 그 종목을 버릴지. 갱신 결정에는 관여하지 않는다.
  `dashboard/service.py`의 `staleness_days=1`은 이 규칙이 하려던 일이므로
  뺐다.
- 값을 못 받는 날(주말·휴장, 그리고 KST 월요일 실행 — 그 시각 뉴욕의 마지막
  마감 세션은 일요일이다)은 종목당 헛도는 요청 한 번이 든다. 휴장 캘린더를
  들이지 않는 값이고, 정확성이 아니라 요청 수로 치른다.

### 검증

`tests/test_rebalance_cadence.py`를 새로 뒀다. 결함이 **모듈 사이 이음매**에
있었으므로 — 로더도 `_is_rebalance_session`도 각자로는 옳았다 — 한 주치 예약
실행을 그대로 밟으며 발동 횟수를 센다. 지금 규칙은 화요일 한 번, 옛 규칙은
화·수·목 세 번(옛 동작도 테스트로 남겼다. 나중에 "덜 받아오자"는 변경이
들어올 때 결과가 문서가 아니라 실행되는 형태로 남아 있도록).

테스트 465건 통과.

---

## 2026-08-28 — 새로고침하면 Overview로 튕기던 것

Trading이나 Audit Log를 보다가 F5를 누르면 Overview로 돌아왔다.

원인은 주소창이었다. 사이드바 항목은 `href="#trading"`을 갖고 있지만 클릭
핸들러가 `preventDefault()`로 그 이동을 막고 메모리 안의 뷰만 바꾸고 있었다.
**해시는 한 번도 바뀌지 않았다** — 그러니 새로고침 때 읽을 주소가 없고, 언제나
기본값인 Overview로 떨어진다. 링크를 복사해 줘도 마찬가지였다.

이제 `setView`가 해시를 함께 맞춘다. 해시를 쓰면 `hashchange`로 다시 들어오는데
그때는 뷰가 이미 같아서 두 번째 통과가 거기서 멈춘다.

**뒤로 가기는 두 경우를 나눴다.** 페이지 간 이동은 히스토리에 쌓아야 뒤로 가기가
동작하지만, 비어 있거나 알 수 없는 해시를 바로잡는 건 `replaceState`로 제자리에
덮어쓴다 — 뒤로 가기가 **애초에 페이지를 가리킨 적 없는 주소**로 걸어 들어가면
안 된다. `#nonsense` 같은 해시는 Overview를 보여주면서 주소도 `#overview`로
정정한다(예전에는 아무 섹션도 매치되지 않아 빈 화면이 됐다).

**검증**: 이 환경의 Chromium이 `libnspr4.so`가 없어 안 뜬다(`sudo apt install
libnspr4 libnss3`가 필요한데 설치하지 않았다). 대신 app.js를 스텁 DOM에 올려
**실제 라우팅 함수를 그대로 실행**해 확인했다: 최초 로드 → `#overview`,
Trading 클릭 → `#trading`, 그 상태에서 새로고침 → Trading 유지, 잘못된 해시 →
Overview로 정정. 화면 렌더링 자체는 브라우저에서 눈으로 한 번 봐 주면 좋겠다.

---

## 2026-08-28 — 리밸런스 요일이 휴장이면 그 주를 통째로 건너뛰고 있었다

"왜 하필 월요일이냐"는 질문에서 나왔다. 주 1회는 DCA의 정체성이지만 **월요일
자체는 근거 없는 기본값**(`rebalance_weekday: 0`)이고, 그걸 확인하다 진짜
문제가 드러났다.

`_mode`는 요일만 비교하고 있었다. `today`는 이미 벤치마크 마지막 봉의 날짜(미국
세션)로 앵커돼 있으므로, **월요일이 미국 휴장일이면 마지막 봉은 금요일이고
요일이 안 맞아 그 주에는 아무것도 사지 않는다.** 2026년 노동절(9/7)이 바로
그 경우다. 그리고 이 실패는 **조용하다** — 신호 0건은 조용한 시장과 구분되지
않는다. 바로 앞 항목의 스냅샷 경보와 같은 종류의 침묵이다.

규칙을 "리밸런스 요일, **또는 그 이후 첫 세션**"으로 바꿨다. 그 주는 하루 늦게
자기 몫의 매수를 받는다.

- **거래 달력은 벤치마크의 봉이다.** 별도 캘린더 피드를 붙이지 않았다 — 이
  전략의 다른 모든 날짜 판단이 이미 그 시계열에서 나오고, 휴장일은 구성상
  이미 빠져 있으며, 별도 피드는 **실제로 거래하는 가격과 어긋날 수 있는**
  두 번째 진실이 된다.
- **보충 매수는 한 번뿐이다.** 앵커 이후 *첫* 세션만 해당한다. 아니면 그 주의
  남은 요일이 전부 리밸런스일로 읽혀 한 주에 서너 번 산다. 테스트로 고정했다.

### 실데이터 영향

2022-01-03~2026-08-26 백테스트: TWR **33.08% → 31.98%**, MDD **28.42% → 27.98%**.
되찾은 주(4.6년간 월요일 휴장 ~20회)만큼 더 샀고, 수익률은 조금 내려가고
낙폭도 조금 줄었다. **성과 개선으로 한 변경이 아니다** — 예정된 적립을 말없이
빠뜨리던 걸 고친 것이고, 숫자가 어느 쪽으로 움직였든 고쳐야 할 동작이었다.

옆에서 본 것: 이 구간 `trade_count`가 0이다(변경 전후 동일). 청산된 왕복이
한 건도 없다는 뜻인데 2022년 하락장에서 추세 이탈 매도가 한 번도 없었다는 건
어색하다. 이번 변경과 무관한 기존 값이라 여기서는 손대지 않고 적어만 둔다.

**검증**: 신규 4개 포함 전체 417개 통과.

---

## 2026-08-28 — 킬 스위치를 화면에 올리고, 조용한 실패를 말하게 했다

LIVE([10]) 앞에 먼저 끝낸 두 가지다. 순서를 이렇게 잡은 이유는 [7]을 [8]보다
앞에 뒀던 것과 같다 — **실거래를 열기 전에 멈추는 수단과 보는 수단이 먼저
있어야 한다.** LIVE로 가면 `KILL_SWITCH` 파일을 손으로 만드는 게 유일한 정지
수단인데, 그건 터미널이 열려 있는 사람만 쓸 수 있는 정지다.

### [12] 제어 화면

헤더의 `PAUSE TRADING`은 그동안 `disabled`였다. 이제 실제로 파일을 만들고
지운다. Trading 화면은 자리표시자에서 상태판으로 바뀌었고, **엔진을 막고 있는
게 무엇인지를 세 칸으로 나눠 보여준다** — 킬 스위치 · `trading.enabled` ·
모드(PAPER, LIVE는 [10] 전까지 닫힘). 하나의 "enabled" 불린으로 합치면
"거래 가능한가"에는 답하지만 답이 '아니오'일 때 **셋 중 무엇을 바꿔야 하는지**를
가린다.

몇 가지는 일부러 비대칭으로 뒀다.

- **발동은 확인 창 없이 한 번의 클릭.** 해제만 확인을 받는다. 멈추는 방향은
  언제나 안전한 쪽이고, 되묻는 정지 버튼은 고장난 정지 버튼이다. 사유 입력도
  선택이다 — 멈춰야 하는 이유가 "뭔가 잘못되고 있다"일 때 변명부터 타이핑하게
  만드는 건 정확히 반대의 거래다.
- **사유는 감사 로그가 아니라 파일 안에도 쓴다** (`reason:` 줄). 이 스위치가
  존재하는 상황이 바로 그것이다 — 한 사람이 한 대에서 멈추고, 다른 사람이(혹은
  일주일 뒤의 같은 사람이) 다른 곳에서 그 파일을 발견한다. 정지는 자기가 있는
  자리에서 스스로를 설명해야 한다.
- **손으로 만든 빈 파일도 똑같이 유효한 정지다.** `touch KILL_SWITCH`가 되는 게
  파일을 쓰는 이유 전체다. 헤더가 없으면 발동 시각을 mtime에서 복원하되
  **화면에 "(파일 수정시각)"이라고 적는다** — 감사 로그의 `changed_at`과 같은
  규칙이다. 없는 정밀도를 지어내지 않는다.
- **감사 로그에는 실제 전이만 남는다.** 이미 발동된 스위치를 다시 발동해도
  아무것도 바뀌지 않았으므로 행을 쓰지 않는다. 이 로그가 가장 중요해지는 날에
  비-사건으로 가득 차 있으면 안 된다.
- **쓰기가 감사보다 먼저다.** Firestore가 안 닿아도 엔진은 멈춰야 한다. 기록
  안 된 정지가, 일어나지 않은 정지의 기록보다 낫다.

### 스냅샷 신선도

8일간 리포트가 안 돈 사고의 교훈은 관측 가능성 쪽이었다. **돌지 않은 작업은
정의상 아무 데도 나타나지 않는다** — 행도, 오류도, 로그도 없다. 그때 유일한
증거는 가장 최신 행의 나이였다. 그래서 대시보드가 그 나이를 소리 내어 말한다.

사이드바에 `Snapshot: Nh ago`가 `Last Sync` 아래 별도 줄로 붙었다. **둘은 다른
시계다** — Last Sync는 이 화면이 토스와 통신한 시각이라 브라우저를 열어 두기만
해도 갱신되고, 8일 내내 초록색이었을 것이다. 조용해질 수 있는 쪽은 스냅샷뿐이다.

임계값은 **26시간**이다. 24시간이 아니라: 작업은 10:00에 돌고 사람은 09:00에
볼 수 있으니 정상적인 평일 간격이 경보를 울리면 안 된다. 넘기면 Overview 상단에
경보가 뜨고 "놓친 날은 소급 생성할 수 없다"는 사실과 확인할 곳(`logs\report_*.log`,
작업 스케줄러)을 같이 적는다.

**스냅샷이 하나도 없는 건 stale이 아니다.** 한 번도 안 돈 것과 돌다가 멈춘 것은
다른 문제이고, 전자는 `snapshot_count`가 이미 말하고 있다.

### 검증

실계좌·실 Firestore로 왕복을 돌렸다. 대시보드에서 발동 → `KILL_SWITCH` 파일에
사유가 적힘 → `python3 trade.py --dry-run`이 `!!! KILL_SWITCH가 활성화되어
있습니다. 모든 신호가 거부됩니다.`로 응답 → 대시보드에서 해제 → 파일 삭제,
감사 로그에 발동·해제 두 행. 신선도는 현재 2.8시간(오늘 10:00 예약 실행분)이라
경보 없음 — 8/28 스케줄러 수정이 계속 살아 있다는 뜻이기도 하다.

신규 10개 포함 **전체 413개 통과**.

### 남은 것

[10] LIVE 전환뿐이다. 그 앞의 실검증은 **월요일(9/1) PAPER 실행** — momentum-DCA는
월요일에만 신호를 내므로 게이트→발주 경로가 실데이터로 도는 첫 날이고, 미해결
#6(매수가능금액이 USD로 잡히는가)이 드러날 자리도 거기다.

---

## 2026-08-28 — 유니버스에 IONQ 편입 (AI 제안 → 사람 승인)

8/28 아침 자동 실행에서 AI 유니버스 검토가 IONQ를 편입 후보로 냈고
(보유 중인 IONX가 2x IONQ ETF다), 사용자가 승인했다. **설계대로 사람이
코드를 고쳐 넣는다** — AI 제안은 리포트와 감사 로그에만 남고 자동 반영되지
않는다.

`DEFAULT_UNIVERSE` 39 → 40종목. `max_weight`는 다른 개별주의 0.35가 아니라
**0.20**으로 더 조였다. 순위 로직이 위험을 못 본다는 뜻이 아니라 — `vol_adjust`가
이미 점수를 실현변동성으로 나눈다 — **그 필터를 뚫고 상위 2위에 드는 주에는
정확히 풀사이즈 포지션이 아픈 주이기 때문**이다. 게이트 한도(`config.yaml`)는
0.25로 뒀다.

### 확인한 것

- **데이터는 2021-01-04부터**다. 그보다 이른 시작일의 백테스트에서는 커버리지
  부족으로 통째로 빠지고, `scripts.backtest`가 "캐시에 없는 심볼(건너뜀)"으로
  **시끄럽게** 알린다. 조용히 빠지지 않는다.
- **2022-01-03~2026-08-26 백테스트**: 전략 TWR 33.08% / MDD 28.42%
  (39종목일 때 33.17% / 27.31% — 사실상 무변화). 균등 DCA는 18.75% → 19.41%로
  올랐다. 즉 IONQ는 **순진하게 다 사는 쪽에 더 도움이 됐고** 전략 성과는
  거의 안 움직였다.
- 다만 예상보다 자주 산다: 그 구간 매수 신호 121건 중 **IONQ가 8건**으로 4번째로
  많다. "변동성 조정이 걸러줄 것"이라는 예상은 절반만 맞았다 — 상승 추세가
  강할 때는 변동성을 나누고도 상위에 든다.
- 2026-08-26 기준 현재 순위는 40종목 중 **27위**(점수 +0.060)라 당장 담기지는
  않는다.

**검증**: 전체 403개 통과. 다음 실행에서 감사 로그에 `universe` 변경 항목이
자동으로 남는다.

---

## 2026-08-28 — 일일 리포트가 8일간 한 번도 안 돌고 있었다 (3중 고장)

"주기적으로 포트폴리오 값과 리포트가 안 생기는 것 같다"에서 출발. 사실이었다.
Firestore 스냅샷이 6건뿐이고 전부 8/26~27 **손으로 돌린** 시각(14:21, 16:09,
20:59…)이었다. 예약 시각인 10:00 기록은 하나도 없었다. 고장이 셋 겹쳐 있었고,
**각각이 앞의 것에 가려져 있었다.**

### 1. 스케줄러에 자리표시자가 그대로 등록돼 있었다

```
Execute   : <run_report.bat
Arguments : 전체 경로> --scheduled
```

문서에 적힌 `<run_report.bat 전체 경로>`를 **그 문자열 그대로** 등록해 놨다.
작업은 매일 10:00에 뜨고 즉시 실패했다(`LastTaskResult=1`). 배치가 시작조차
못 하니 `logs\report_*.log`도 한 번도 안 생겼고, **아무 로그가 없다는 사실
자체가 유일한 증거였다.** 8/20 WORKLOG의 "스케줄러로 실제 1회 실행 →
LastTaskResult=0" 검증은 그 뒤 어느 시점에 등록이 덮인 것으로 보인다.

같이 어긋나 있던 설정 둘도 문서화된 의도로 되돌렸다: `StartWhenAvailable`이
꺼져 있었고(PC가 꺼져 있어 놓친 실행을 영영 따라잡지 못한다 — 스냅샷은 소급
생성이 불가능하다), `ExecutionTimeLimit`이 30분이 아니라 기본값 72시간이었다.

### 2. Windows 파이썬에 `google-cloud-firestore`가 없었다

경로를 고치자 다음 오류가 드러났다: `ImportError: cannot import name
'firestore' from 'google.cloud'`. 8/26 SQLite→Firestore 이관 때 **WSL 쪽에만**
설치했다. 개발은 WSL, 스케줄러는 Windows 파이썬
(`AppData\Local\Programs\Python\Python312`)이라 환경이 둘인데, 그 사실이
어디에도 안 적혀 있었다. 이 상태는 조용하다 — 손으로 돌리면 되고 스케줄러만
매일 죽는다.

### 3. `.env`의 크리덴셜 경로가 WSL 표기였다

그러자 세 번째가 나왔다: `GOOGLE_APPLICATION_CREDENTIALS`가
`/mnt/c/Users/...`로 적혀 있어 Windows 파이썬이 못 연다. 한쪽 표기로 고치면
반대쪽이 깨지므로 **코드가 풀도록** 했다 — `config.resolve_service_account()`가
적힌 경로 그대로 → 반대 OS 표기 → 저장소의 `secrets/` 사본 순으로 찾는다.
전부 실패하면 None을 반환하고 환경변수를 **건드리지 않는다**: 그래야 google.auth가
사용자가 실제로 적은 경로를 그대로 오류에 담아 준다. 고칠 수 있는 건 그 경로다.

### 결과

스케줄러 실행 → `LastTaskResult=0`. 스냅샷 6행째 적재(23,550,529원, −32.00%),
뉴스 4섹션, AI 애널리스트, 유니버스 검토(보류 0건·후보 IONQ), Notion 리포트
생성까지 전 구간이 처음으로 자동으로 돌았다. 감사 로그의 실제 기준선 항목도
이때 처음 기록됐다.

**교훈은 관측 가능성 쪽이다.** 이 셋 중 어느 것도 대시보드나 리포트에 드러나지
않았다 — 리포트가 "안 생긴다"는 건 정의상 아무 데도 안 나타난다. 확인 명령을
README에 적어 뒀지만, 근본적으로는 마지막 스냅샷이 24시간보다 오래됐을 때
대시보드가 그걸 말해 줘야 한다(미착수).

이관 전 `stocklens.db`도 확인했다 — 전 테이블 0행이라 유실된 이력은 없다.

**검증**: 신규 4개 포함 전체 403개 통과.

---

## 2026-08-27 — 감사 로그에 "변경 시각"을 붙였다 (되찾을 수 있는 만큼만)

바로 앞 항목에서 감사 로그는 감지 시각(`detected_at`)만 갖고 있었다. "변경
시각으로는 못 하나?"에 대한 답: **되찾을 수 있는 경로가 두 개 있고, 정확도가
다르므로 어느 쪽인지 같이 적어야 한다.**

- **git** — 설정이 추적 중이고 수정되지 않은 소스 파일에 있으면, 그 파일을 건드린
  마지막 커밋이 **정확한 시각과 실제 작성자**를 준다. `trading.universe`가 비어
  있으면 전략은 `DEFAULT_UNIVERSE`를 쓰는데 그건 코드다 — 즉 유니버스 변경은
  git이 정확히 날짜를 매길 수 있는 쪽에 속한다. actor가 추정이 아니라 사실이
  되는 유일한 경우.
- **mtime** — `config.yaml`은 시크릿 때문에 일부러 git 밖에 있다. 파일 마지막
  저장 시각이 최선이고, 1인 편집에는 충분하지만 **그 파일 전체의 마지막
  저장**이다: 유니버스를 토요일에, Notion 토큰을 일요일에 고쳤으면 일요일이
  찍힌다. 백업 복원이나 `touch`도 움직인다.
- **없음** — 파일을 못 읽으면 `changed_at`은 None이고 화면에 "변경 시각 불명"으로
  나온다. **감지 시각을 그 자리에 채워 넣지 않는다.** 읽는 사람이 "변경 시각"으로
  읽을 칸에 다른 값을 넣는 건 없는 정밀도를 지어내는 짓이다.

작업 트리가 더러우면 git 경로를 안 쓰고 mtime으로 내려간다. **커밋 안 된 로컬
수정이 있는 파일의 마지막 커밋 시각은 변경 시각이 아니다** — 그걸 그대로 적는 게
이 로그가 저질러선 안 되는 유일한 종류의 오류라서, `git status --porcelain`으로
먼저 확인한다.

AI 보류·제안은 `changed_by_method: "direct"`다. 검토가 방금 만든 항목이라 변경과
감지 사이에 간격이 없다 — 되찾을 것이 없는 유일한 actor.

정렬은 계속 `detected_at` 기준이다. 항상 존재하고 단조롭게 증가하는 유일한
필드라, 변경 시각으로 정렬하면 오래된 mtime을 가진 항목이 뒤늦게 목록 중간에
끼어든다.

화면에는 큰 글씨로 변경 시각, 그 아래 작은 줄에 `actor · 파일 · 근거 · 감지 시각`.
시각에 마우스를 올리면 그 근거가 뭘 의미하는지 설명이 뜬다.

**검증**: 네 경로 전부 실물로 확인 — `src/strategy/universe.py`는 git
(`2026-08-27T21:45:07`, 작성자 `jujeong`), `config.yaml`은 mtime, AI 항목은
direct, 없는 파일은 불명. 신규 5개 포함 전체 399개 통과.

---

## 2026-08-27 — 감사 로그: 누가 언제 뭘 바꿨나

유니버스와 전략 설정이 바뀌었을 때 기록이 남는 곳이 없었다. `config.yaml`은
git에도 안 올라가고(시크릿 포함), 39종목으로 넓힌 것도 WORKLOG에 산문으로
적힌 게 전부다.

### 선언이 아니라 감지

아무도 감사 항목을 손으로 쓰지 않는다. 그래서 아무것도 요구하지 않게 만들었다 —
매 실행이 **엔진이 무엇을 거래할 수 있는지를 결정하는 설정**(유니버스·전략
목록·전략 파라미터·리스크 한도)의 지문을 떠서 직전 지문과 비교하고, 차이를
기록한다(`src/audit.py`). `config.yaml`을 고치기만 하면 다음
`python main.py`나 `python trade.py`가 알아서 잡는다. 두 프로세스가 같은
소스(config)로 지문을 뜨기 때문에 먼저 도는 쪽이 기록하고 나중 쪽은 조용하다.
`--dry-run`은 "기록 없음"을 약속하므로 건너뛴다.

지문은 **전부 문자열**로 눕힌다. YAML에서 읽은 int, `Decimal("0.35")`,
Firestore에서 돌아온 `"0.35"`가 전부 같게 비교돼야 한다 — 왕복 한 번에
"변경됨"이 뜨는 로그는 읽는 사람이 무시하도록 학습시킨다.

### 스키마가 주장하지 않는 것 두 가지

- **`detected_at`이지 `changed_at`이 아니다.** 시스템이 처음 본 시각만 알 수
  있다. 토요일에 고치고 월요일에 돌리면 월요일로 찍히고, 필드 이름이 그 사실을
  말해야지 없는 정밀도를 암시하면 안 된다.
- **사람 actor는 추정, AI actor는 사실이다.** 설정 변경은 그걸 처음 발견한
  프로세스의 OS 사용자(`user@host`)로 적는다 — 1인 머신에선 대개 맞지만
  **보안적 주장이 아니다.** 이 대시보드엔 인증이 없다. 반면 AI 보류는 그 검토가
  직접 쓴 것이라 정확하다. UI 배지 색도 둘을 갈라 놨다.

### 첫 실행에 39줄 대신 한 줄

지문이 아예 없는 첫 실행에서 유니버스 전체를 "추가 39건"으로 적으면 사실이지만
쓸모가 없다. 대신 `baseline` 항목 한 줄("감사 로그 시작 — 유니버스 39종목 ·
전략 1개")을 쓴다. 아무 기록도 안 쓰면 페이지가 텅 비어서 **로그가 고장 난 것처럼
보이는데**, 그건 "아직 아무것도 안 바뀌었다"와 다른 인상이다.

### AI가 한 일도 같은 로그에

`universe_review`의 보류와 후보 제안도 감사 항목으로 들어간다. 단 **따로 두
줄**이다 — 하나는 엔진 동작을 이미 바꿨고 하나는 아무도 실행하지 않은 제안이라,
한 줄로 합치면 리포트 레이아웃에서 애써 없앤 혼동을 감사 로그에서 되살리게 된다.

### 페이지

대시보드에 **Audit Log** 탭 추가. 카테고리 필터(전체·유니버스·파라미터·리스크
한도·AI 보류·AI 제안), 항목마다 `before → after`와 AI 보류의 인용 근거까지
펼쳐 보여준다. 상단에 "이 시각은 변경 시각이 아니라 감지 시각"이라고 못박아 뒀다.

`recent_audit(category=...)`는 **정렬만 Firestore에 밀고 카테고리는 파이썬에서**
거른다. `where` + 다른 필드 `order_by`는 배포마다 복합 인덱스가 필요한데,
**에뮬레이터는 그 쿼리를 그냥 받아준다** — 즉 실패가 프로덕션에서 처음 나타난다.
이 모듈의 다른 범위 스캔이 전부 같은 이유로 파이썬에서 거른다.

`record_config_changes`는 절대 안 죽는다. 일일 리포트를 멈출 수 있는 감사 로그는
처음 멈추는 날 삭제당하고, 그러면 리포트도 로그도 없다.

**검증**: 실 Firestore로 왕복 확인 — 첫 실행 기준선, 무변경 실행 침묵, XOM 제거 +
`top_n` + `max_orders_per_day` 편집을 3개 카테고리로 정확히 잡았고, 대시보드
`/api/audit`이 최신순·필터까지 응답했다. (확인용으로 넣은 항목은 지웠다.)
신규 21개 포함 전체 394개 통과.

---

## 2026-08-27 — AI에게 거부권과 후보 제안권을 준다 (매수권은 안 준다)

"AI가 유망 종목을 캐치해서 유니버스를 정기 갱신하면 어떨까"에서 출발했는데,
그대로 가면 **바로 앞 항목에서 없앤 편향을 더 나쁜 형태로 되살린다.** LLM은
학습 데이터에 2026년까지 들어 있어서 "2015년 시점에 유망한 종목"을 물으면
엔비디아가 어떻게 됐는지 이미 알고 답한다. 그런 유니버스로 만든 백테스트
숫자는 검증이 원천적으로 불가능하다 — 미래를 모르는 상태로 얼린 모델이
있어야 하는데 그런 건 없다.

그래서 AI에게 **순위를 매기는 일은 주지 않았다.** 그건 백테스트되는 규칙의
몫이다. 대신 규칙이 원리상 볼 수 없는 것 두 가지만 맡겼다.

### 1. 거부권 — 자동 적용되지만, 뺄 수만 있다

`src/universe_review.py`. 매일 리포트 때 Gemini에게 유니버스와 그날 헤드라인을
주고 "신규 매수를 보류해야 할 종목"을 묻는다. 통과 조건이 좁다:

- **허용 카테고리 6개뿐** — 상장폐지·피인수/합병·티커변경·거래정지·회계부정
  조사·파산. "밸류에이션이 부담" "실적 부진" "전망 어두움"은 전부 폐기된다.
  그건 전략이 할 일이고, 전략은 검증할 수 있다.
- **근거 인용 필수** — 오늘 준 헤드라인 중 하나를 `evidence`에 적어야 한다.
  비어 있으면 폐기. 이유를 못 대는 모델은 추측하는 중이다.
- **유니버스 안에 있는 심볼만** — 모르는 티커를 지어내도 무시된다.
- **1회 최대 3건**(`max_vetoes`) — 모든 게 잘못됐다고 판단한 모델이 전략을
  통째로 정지시킬 수 없어야 한다.
- **7일 후 자동 해제**(`veto_ttl_days`) — 배치가 멈춘다고 AI의 어제 의견이
  영원히 살아 있으면 안 된다. 다음 날 같은 보류를 다시 내면 만료가 밀린다.
- **매도는 절대 불가** — 리스크 게이트의 새 규칙 `symbol-vetoed`는 `is_buy`
  일 때만 걸린다. 보류는 노출을 더하지 말 이유이지, 뉴스가 만든 가격에 포지션을
  던질 이유가 아니다.

적용 지점은 유니버스 필터가 아니라 **리스크 게이트**다. 필터로 빼면 아무
기록이 안 남지만, 게이트에서 거부하면 "이 종목을 사려 했는데 보류가 막았다"가
`signals` 컬렉션에 남는다 — 나중에 이 거부권이 쓸모 있었는지 **채점할 수 있다.**

전략은 이 목록을 못 본다(`ctx.blocked_symbols`는 게이트만 읽는다). 전략이
읽기 시작하면 순수성이 깨지고 백테스트와 실거래가 갈라진다.

### 2. 후보 제안 — 자동 반영은 없다

유니버스 밖 대형주를 최대 5개까지 이유와 함께 제안한다. Firestore와 Notion
리포트에 기록될 뿐 **아무것도 사지 않는다.** 편입은 사람이 `config.yaml`을
고치는 일로 남는다. 리포트에서도 "자동 적용됨"과 "자동 반영 안 됨"을 서브헤딩으로
갈라 놨다 — 읽는 사람이 둘 중 뭐가 이미 엔진을 바꿨는지 추론하게 두면 안 된다.

**비대칭이 요점이다.** 틀린 보류는 한 종목의 며칠치 기회비용이지만, 틀린
매수는 돈이고, AI가 조용히 고쳐 쓴 유니버스는 어떤 백테스트도 설명하지 못하는
전략이다.

### 실물 확인

실제 Gemini 호출로 확인했다. 평범한 날의 실뉴스로는 **보류 0건**(정상 —
대부분의 날은 빈 목록이 정답), 후보는 ABBV·LIN·NEE 등 섹터를 넓히는 이름이
나왔고 두 번 돌려도 대체로 같았다. "인텔 나스닥 거래정지 + 회계 조사" 헤드라인을
넣자 `INTC [trading_halt]`가 헤드라인을 인용하며 잡혔고, 같은 뭉치에 있던
"디즈니 실적 호조"는 건드리지 않았다.

`trade.py`는 실행 시 활성 보류를 **종목명까지** 찍는다 — "2종목 보류"라고만
쓰면 어느 둘인지 읽는 사람이 짐작해야 한다.

**나중 과제**: 규칙 기반 유니버스 자동 갱신(예: 분기마다 대형주 100개 중 12개월
수익률 상위 40개로 교체). 이건 백테스트가 되고 재현도 된다 — AI가 아니라 규칙이
하기 때문이다.

**검증**: 신규 24개 포함 전체 371개 통과.

---

## 2026-08-27 — 계획 8단계: 백테스트 검증, 그리고 유니버스 확장

전략을 실제 시세로 처음 돌렸다. **헤드라인 숫자는 아무것도 증명하지 않았고,
비교가 결론을 뒤집었다.**

### 1. 전략만 보면 훌륭해 보인다

2012-05-18~2026-08-26, 초기 100만 + 월 75만 적립:
TWR 33.92% · IRR 36.87% · **MDD 57.67%** · 신호 449건(weekly 438 · trend-exit 9
· dislocation 2) · 리스크 게이트 거부 0건. 2022년 한 해 −55.31%.

### 2. 같은 종목을 그냥 사면 더 낫다

| | TWR | MDD |
|---|---|---|
| 모멘텀 전략 | 33.92% | 57.67% |
| QQQ DCA | 19.81% | 35.12% |
| QLD DCA (2x QQQ) | 33.31% | 63.68% |
| 유니버스 12종목 균등 DCA | **38.14%** | **55.56%** |
| M7 7종목 균등 DCA | **42.36%** | 58.22% |

수익률도 낮고 낙폭도 크다. 2016·2020 시작도 같고, 2022(고점) 시작에서만
뒤집힌다(36.7%/29.8% vs 21.6%/46.7%) — 전략의 값어치는 수익이 아니라 **고점
진입 시 낙폭 회피**였다.

### 3. 원인은 유니버스였다 — 순위 매길 것이 없었다

12종목은 2026년에 승자를 알고 고른 목록이다. 전부 승자인 목록에서 상위 2개를
고르는 건 집중일 뿐 선별이 아니다. 그래서 2012년 당시 누구나 들고 있었지만
승자가 아니었던 12종목(INTC·CSCO·IBM·ORCL·QCOM·DIS·KO·PFE·XOM·T·GE·WMT)을
섞어 24종목으로 다시 돌렸다:

| 24종목 | 전략 | 균등 DCA |
|---|---|---|
| 2012~ | **31.12%** / 56.51% | 30.26% / 49.19% |
| 2016~ | **29.39%** / 44.93% | 27.17% / 42.51% |

**여기선 전략이 이긴다.** 실제로 GE(2018 이후 턴어라운드)를 두 번째로 많이
사고 IBM·QCOM은 거의 안 산다. 순위 로직은 작동한다 — 거절할 패자가 목록에
있을 때만.

그래서 `DEFAULT_UNIVERSE`를 12 → **39종목**으로 넓혔다(반도체·소프트웨어·
금융·헬스케어·소비재·에너지/산업재, 전부 2010년 이후 일봉 보유). 미래에 누가
승자인지는 알 수 없다는 것이 요점이므로, 지금 승자로 보이지 않는 이름을
일부러 남겨 두었다. `config.yaml`·`config.example.yaml`의
`max_position_weight_overrides`도 39종목으로 맞췄다 —
`_require_weight_caps_fit_the_gate`가 로드 시점에 요구한다.

### 4. 확장 유니버스에서의 최종 성적

| 시작 | 전략 TWR/MDD | 균등 DCA TWR/MDD |
|---|---|---|
| 2012 | 32.43% / 51.39% | 30.15% / 43.31% |
| 2016 | 29.88% / 44.25% | 27.83% / 37.25% |
| 2020 | 16.52% / 28.22% | 24.63% / 31.45% |
| 2022 | 33.17% / 27.31% | 18.75% / 28.82% |

수익률은 4번 중 3번 이기고, 낙폭 대비로는 4번 중 3번 진다. **작고 일관되지
않은 우위**이고, 시작 시점에 크게 좌우된다. 2020 시작이 최악인 건 코로나
급락 직후 추세 이탈로 빠졌다가 반등을 놓쳤기 때문 — 추세추종의 전형적 대가다.
확실히 나아진 것 하나: 유니버스를 넓히자 MDD가 전 구간에서 내려갔다
(57.7→51.4, 50.8→28.2).

### 5. 파라미터 (4개 시작일 × 12개 변형)

과최적화는 없다 — 애초에 맞춘 적이 없다. 표면도 평평하다(스파이크 없음).
방향이 일관된 건 둘뿐: `vol_adjust=off`(수익 +2~4%p, MDD도 ↑ — 알려진
"변동성 조정이 레버리지를 상쇄" 문제), 단기 lookback(21/63)(4구간 모두 수익 ↑).
**둘 다 반영하지 않았다** — 겹치는 4개 구간은 독립 표본이 아니고, 한 경로에
맞추는 순간 지금까지 없던 과최적합이 생긴다. 시도한 조합은 위 12개가 전부다.

부수 발견 2건:

- **`leverage_requires_trend`는 매수 경로에서 무의미하다.** 꺼도 14년치 결과가
  1원도 안 바뀐다. 추세 이탈일 600일 중 83%는 변동성 게이트(≤0.35)도 통과하니
  게이트가 겹친 게 아니라, 하락장에선 레버리지 ETF의 모멘텀 점수가 이미 음수라
  후보에서 빠진다. 실제 하방 방어는 `min_score`와 trend-exit 매도 둘뿐이다.
- **`dislocation`은 14년간 2번 발동했다.** 사실상 죽은 코드.

### 6. OCO 손절 — 이제 데이터로 답이 나왔다 (계획 [9] 후속)

전략에 손절 파라미터 자체가 없고, 백테스트는 손절 폭을 정할 근거를 주지
않는다. 실현 손실은 미미하고(평균 −23~−47달러) 손실은 전부 "들고 버티는
구간"에서 난다 — 2x ETF에 −15% 스톱을 걸면 방어보다 조기 청산이 압도적이다.
**OCO는 계속 꺼둔다.** 근거가 문서에서 데이터로 바뀌었을 뿐 결론은 같다.

### 7. 도구에 남긴 것

- `src/backtest/benchmarks.py` — 같은 기간·같은 적립의 균등 DCA/벤치마크 DCA
  곡선. `scripts.backtest`가 **기본으로** 같이 출력한다(`--no-compare`로 끔).
  전략 자기 CAGR만 찍는 리포트는 잘못된 질문에 답한다 — 이번에 결론을 바꾼 건
  전부 이 비교였고, 다음 사람이 이걸 다시 발견하게 두면 안 된다.
- `scripts/backtest.py` 크래시 수정: 손실 거래가 0건이면 `avg_loss_usd`가
  None인데 `.1f`로 포맷하다 TypeError. 확장 유니버스에서 실제로 터졌다.

**검증**: 신규 5개 포함 전체 347개 통과.

---

## 2026-08-27 — 일봉 캐시를 Firestore에서 로컬 SQLite로

계획 8단계(백테스트 검증)를 시작하자마자 `--refresh`가 **Firestore 무료 티어
일일 쓰기 할당량(20,000건)에서 멈췄다.** 12종목 × 16년 ≈ 5만 건. 4종목
(AAPL·AMZN·GOOGL·META)까지 들어가고 429 Quota exceeded.

`36879eb`에서 SQLite→Firestore 전면 이관할 때 일봉 캐시까지 딸려 간 게
원인이다. 일봉은 (심볼, 세션)당 한 행짜리 **불변 참조 데이터**다 —
대시보드도 리포트도 안 읽고, 다른 기기도 안 읽는다. 게다가 `--offline`
재현성이 목적이라면 "복사할 수 있는 파일"이 "온라인이어야 닿는 컬렉션"보다
낫다. `src/data/cache.py`를 로컬 SQLite(`bars.db`, `M7_BAR_CACHE_PATH`로
경로 변경 가능)로 되돌렸다. 주문·신호·스냅샷 등 **라이브 데이터는 Firestore
그대로** — 작고, 가변이고, 대시보드가 실제로 공유한다.

`coverage()`는 별도 테이블 대신 `MIN/MAX`로 유도한다. 저장해 둔 범위는 그
범위가 설명한다고 주장하는 행들과 어긋날 수 있지만, 유도한 값은 어긋날 수
없다.

부수 효과로 `tests/test_bar_cache.py` 11건이 Firestore 에뮬레이터(그러니까
firebase-tools + JRE) 없이 돈다 — `tmp_path`에 파일 하나면 된다.

**검증**: 전체 342개 통과. `--refresh` 재실행으로 12종목 2010-01-04~2026-08-26
전량(약 5만 봉) 수 초 만에 적재. 할당량·비용 0.

> Firestore `daily_bars`에 먼저 들어간 4종목 16,700여 건은 그대로 남아 있다.
> 지우는 것도 삭제 할당량을 먹어서 오늘은 두었다.

---

## 2026-08-27 — 종목 뉴스 검색을 상품명 대신 기초자산으로

레버리지/단일종목 ETF의 정식 상품명("DIREXION DAILY TSLA BULL 2X SHARES")으로 뉴스를 검색하면 ETF 발행사 보도자료 위주로 걸리고 정작 그 종목에 영향을 주는 기사는 잘 안 잡혔다.

`ManualHolding.underlying`(config.yaml `portfolio.manual[].underlying`) 추가 — TSLL은 "TSLA", IONX는 "IONQ"처럼 실제로 추종하는 기초자산을 명시. `Position.underlying`으로 흘러가고, `portfolio_keywords()`가 있으면 이걸, 없으면 기존처럼 표시 이름을 키워드로 쓴다. 상품명에서 기초자산을 문자열 파싱으로 추측하지 않고 config에 명시하도록 한 건 — universe.py가 레버리지 상품의 `underlying`을 이미 같은 방식(명시, 추측 금지)으로 다루는 것과 일관되게.

**검증**: 키워드가 "DEFIANCE DAILY TARGET 2X LONG IONQ ETF" → "IONQ", "DIREXION DAILY TSLA BULL 2X SHARES" → "TSLA"로 바뀌었고, 실제로 IonQ 개별 기사(우주궤도 광통신 단말기, 이사진 확충)와 테슬라 개별 기사(중국 리콜, 사이버트럭 가격 인상)가 잡혔다 — 이전엔 ETF 발행사 뉴스만 나왔다. 신규 테스트 6개 포함 전체 342개 통과.

---

## 2026-08-27 — AI Analyst: 종목별 뉴스 반영 + 외부 안전자산(적금) 고려

### 1. AI 프롬프트가 종목별 뉴스를 안 읽고 있었다

바로 앞 항목에서 리포트에 종목별 뉴스 섹션(`news_data["keywords"]`)을 추가했는데, `Analyst.analyze_portfolio`는 여전히 `news_data["general"]`(일반 경제뉴스)만 프롬프트에 넣고 있었다. `_keyword_news_str()` 추가 — 키워드(매크로 + 보유종목 이름) 각각의 헤드라인을 프롬프트에 넣고, "헤드라인을 그대로 반복하지 말고 그 종목에 대해 뭘 의미하는지 요약하라"고 명시.

### 2. 계좌 밖 안전자산(적금)을 전략에 반영

사용자가 매달 50만원씩 청년미래적금을 넣고 있는데, AI가 증권 계좌만 보고 "안전자산도 좀 담으세요" 식으로 조언하면 실제 총자산 배분과 어긋난다. `config.yaml`에 `portfolio.savings`(이름·월 납입액·종류) 섹션 추가 — `ManualHolding`과 달리 시세 조회 대상이 아니라 AI에게 넘길 사실 하나일 뿐이라 `Position`/포트폴리오 집계엔 안 들어가고 `Analyst`로만 흘러간다.

프롬프트에 "이미 계좌 밖에 안전자산 완충이 있으니, 그것만으로 현금성 자산을 더 담으라고 권하지 말고 증권 계좌의 공격성이 그 안전망 대비 적절한지 판단하라"고 지시.

**검증**: 실계좌 + 실 Gemini 호출로 확인 — AI가 실제로 "안전자산 적금이 마련되어 있는 만큼 증권 계좌는 성장형 자산을 공격적으로 운용해도 된다"는 취지로 반영했고, IONX/TSLL 각각의 종목 뉴스(양자컴퓨팅, 테슬라 관심)를 근거로 언급했다. 신규 테스트 12개 포함 전체 336개 통과.

---

## 2026-08-27 — 리포트: 보유 종목별 뉴스

리포트의 "📰 Economic News"가 `config.yaml`에 손으로 적은 매크로 키워드(반도체·환율)만 검색했다. 실제 보유 종목 관련 기사는 없었다.

`NewsFetcher`는 이미 키워드별 섹션을 처리할 수 있었고(`news_data["keywords"]`), Notion 렌더링도 키워드마다 `News: {keyword}` 서브헤딩을 이미 그리고 있었다 — 빠진 건 "보유 종목 이름을 키워드에 넣어주는 것"뿐이었다.

`src/news.py`에 `portfolio_keywords(snapshot)` 추가 — 보유 포지션의 표시 이름(티커가 아니라 이름: "005930"보다 "삼성전자"가 Google News에서 훨씬 잘 걸림)을 중복 없이 뽑는다. `main.py`가 이걸 `config.news_keywords`에 이어붙여 `NewsFetcher`에 넘긴다.

**검증**: 실제 계좌로 실행 — IONX(2x IonQ ETF), TSLL(2x TSLA) 둘 다 관련 기사가 잡혔다(TSLL은 테슬라 주주 구조 기사, IONX는 디파이언스 ETF 기사). 신규 테스트 4개 포함 전체 324개 통과.

**다음 후보**: AI Analyst 프롬프트(`src/analyst.py`)는 아직 `news_data["general"]`만 보고 종목별 뉴스는 안 읽는다 — 원하면 같이 넣을 수 있음.

---

## 2026-08-27 — 매매전략 Opus 재검토 반영 (LIVE 전 필수 6건)

전략을 실거래에 붙이기 전 Opus 어드바이저로 한 번 더 검토했다. "반드시 고칠 것" 6건 중 4건 수정, 1건은 문서화로 결론, 1건은 검증 과제로 남김.

### 1. 디슬로케이션 쿨다운이 라이브에서 죽어 있었다

급락 시 예산 2배 매수("디슬로케이션") 후 5일 쿨다운을 거는 로직이, 라이브에서는 마지막 디슬로케이션 매수를 **한 번도 인식하지 못했다.** 라이브 스토어(`store.repo.save_decision`)는 `signal.meta`를 dict 그대로 `payload` 키에 저장하는데, `_entry_meta`는 그걸 JSON 문자열로 보고 `json.loads(dict)` → TypeError → `{}`로 떨어뜨렸다. 백테스트는 `meta` 키를 써서 이 경로를 안 탄다 — 즉 백테스트가 검증하던 코드와 라이브가 실행하던 코드가 달랐다. `payload`가 이미 dict면 그대로 반환하도록 수정.

### 2. `ctx.recent` 크기가 백테스트/라이브 불일치

라이브는 `store.recent_signals(limit=50)`, 백테스트는 무제한 `recent_log`를 넘겼다. 쿨다운의 "로그가 충분히 안 옛날까지 닿으면 fail-closed" 분기는 50개 창에서만 의미가 있는데, 백테스트에선 절대 안 걸렸다. 백테스트도 `recent_log[-50:]`로 맞춤.

### 3. 추세 이탈 청산이 매일 중복 발행

`_exit_signals`는 매 실행마다 백지에서 다시 판단한다. 하락 추세가 며칠 이어지면 매일 `position.quantity` 전량 매도를 새로 냈다. 라이브는 T+1~T+2 정산 지연 동안 포지션·매도가능수량이 그대로라, 리스크 게이트도 이 중복을 못 막는다. `exit_cooldown_days`(기본 3일) 파라미터와 `_exit_in_flight()` 가드 추가 — 최근 청산 신호가 승인된 적 있으면 재발행 억제. `ctx.recent`가 순수 전략이 읽을 수 있는 유일한 in-flight 상태다.

### 4. 리밸런스 요일이 KST 벽시계 기준

`ctx.now.date().weekday()`로 "월요일 리밸런스"를 판정했다. 미국장 마감은 KST 화요일 새벽 — 마감 후 배치가 돌면 이미 화요일이라 그 주 리밸런스를 **통째로 건너뛴다.** `today`를 `benchmark_history.last_date`(= 미국 세션 날짜)에 고정. 백테스트에선 `ctx.now`의 날짜와 항상 같아 무변화. `now`를 데이터 마지막 봉과 분리해 놨던 모멘텀 테스트 ~8개를 마지막 봉이 실제로 월요일에 떨어지도록 재작성.

### 5. OCO 손절은 켜지 않는다 — 문서로 명시 (결론)

[9]에서 만든 reconciler의 OCO 등록은 `momentum-dca` 매수에 대해 **한 번도 실행되지 않는다.** 전략이 `stop_loss_price`/`take_profit_price`를 신호에 안 싣기 때문. 손절 폭 자체가 계획서 8단계 백테스트가 정해야 할 숫자라, 검증 안 된 값(예: -15%)을 넣으면 손실을 막기보다 멀쩡한 포지션을 조기 청산할 위험이 더 크다. 지금은 켜지 않고, 현 상태를 세 곳에 분명히 적었다:

- `momentum_dca.py` 모듈 docstring — "per-position 손절 없음, 추세 필터가 유일한 하방 방어, 봇이 돌 때 + 벤치마크 전체 추세가 꺾일 때만 작동" + "8단계 나오면 재검토"
- `reconciler.py` 모듈 docstring — "'리컨실러가 OCO를 건다'를 '라이브 매수가 손절로 보호된다'로 읽지 말 것 — 현재는 아니다"
- `_arm_bracket` early-return 주석

### 6. USD 매수 여력 — 검증 과제 (미해결)

`_budget`이 `ctx.buying_power["USD"]`를 읽는데, 토스 계좌가 원화만 보유하고 자동 환전이 안 되면 예산이 영원히 0 → 전략이 조용히 아무 주문도 안 낸다. 실제 잔고로 PAPER 1회 실행해 확인 필요. 로그인 정보가 있어야 해서 이번엔 못 함.

**검증**: 신규 3개 포함 전체 315개 통과.

### 다음

- [10] 전에 실계좌로 `python trade.py` 1회 — `ctx.buying_power`에 USD가 잡히는지 확인
- 8단계 백테스트에서 손절 폭 결정 후 OCO 재검토

---

## 2026-08-27 — UI: 대시보드 폭 + 수동 보유분 당일 손익

### 1. Overview가 화면을 안 채움

`main` 안쪽 컨테이너가 `max-w-container-max`(1600px) + `mx-auto`라서 와이드 모니터에서 양옆이 비었다. `w-full`로 교체. 내부 그리드는 원래 `xl:grid-cols-*`라 그대로 늘어난다. `docs/ui/mockup.html`도 동일하게.

### 2. Today's P&L이 수동 보유분에서 항상 +0

원인: 현재 포트폴리오(IONX, TSLL)는 전부 `config.yaml`의 수동 입력. 토스 `/api/v1/holdings`는 `items: []`. 토스는 자기 계좌 종목엔 `dailyProfitLoss`를 계산해 주지만, 수동 보유분은 `ManualSource`가 그 필드를 안 채웠고 채울 수도 없었다 — 토스 시세 API(`/api/v1/prices`)가 `lastPrice`만 주고 전일 종가를 안 준다.

옵션 B(yfinance 전일 종가)로 해결:

- `ManualSource(previous_closes={symbol: Decimal})` 주입 → 라이브 시세가 있을 때 `qty × (last − prev)`로 `daily_profit_loss`/`_rate` 산출. 없으면 `None`(0 아님, 집계에서 제외).
- `PortfolioService(previous_close_fn=...)` 훅. 호출 실패는 전부 `except Exception`으로 삼켜서 스냅샷·대시보드가 절대 안 죽음. **대시보드에만** 연결(리포트·매매 경로는 그대로).
- `src/dashboard/service.py`의 `_make_previous_close_fn()` — 공유 Firestore 봉 캐시(`BarCache`) 우선, 부족분만 yfinance(`staleness_days=1`). 지연 생성이라 import 시점엔 yfinance도 Firestore도 안 건드림. `_exchange_today()`는 뉴욕 날짜 기준(KST 저녁에 "오늘"이 하루 빨리 넘어가 0 되는 것 방지). 전일 종가 = 뉴욕 오늘보다 이전인 마지막 완료 봉.

### 3. `BarCache.bars`가 복합 인덱스를 요구하던 문제

`where(symbol ==) + where(date 범위) + order_by(date)`는 Firestore 복합 인덱스가 필요한데 `quant-81f19` 프로젝트에 없었다(서비스 계정은 인덱스 생성 권한도 없음). `symbol` 등호만 쿼리로 보내고 날짜 범위·정렬은 파이썬에서 — `store.repo`가 자기 범위 스캔을 처리하는 방식과 동일. 이제 복합 인덱스 없이 동작하고, 전략 히스토리 로더도 같이 덕을 본다.

**검증**: `yfinance` 설치 후 실데이터로 확인 — IONX +497 USD(+6.3%), TSLL +53 USD(+0.7%), 당일 합계 **+759,511 KRW (+3.5%)**. 전체 320개 테스트 통과(신규 5).

---

## 2026-08-27 — Phase 2 [9]: reconciler + OCO 손절

체결을 아무도 안 읽는 상태였다. `python trade.py --reconcile`로 미체결 LIVE 주문의 체결을 확인하고, 매수 체결이 확인되면 OCO 손절을 등록한다. 설계 2.3이 "개인 자동매매에서 가장 중요한 기능"이라고 꼽은 부분 — 손절을 토스 서버에 위임해서 봇이 죽어도 손절이 산다.

### 1. 왜 executor가 아니라 reconciler가 OCO를 건다

`Signal.stop_loss_price`/`take_profit_price`는 [6]부터 자리만 잡혀 있었다. 자연스러운 자리는 executor일 것 같지만 — **주문을 낸 시점엔 아직 체결이 아니다.** 미체결 주문에 손절을 거는 건 의미가 없고, 부분체결이면 그 수량만큼만 걸어야 한다. "주문을 냈다"와 "주식을 실제로 가졌다"를 한 자리에서 섞으면 이 둘의 경쟁 상태를 감수해야 한다.

그래서 신호의 손절가가 주문 문서에 그대로 실려 저장되고(`save_order`가 `intent.signal`에서 복사), reconciler가 체결을 확인한 **다음에만** 등록한다.

### 2. 체결 여부를 상태 문자열이 아니라 수량으로 판정

토스가 실제로 어떤 상태 문자열을 쓰는지(`FILLED`? `COMPLETED`? `체결완료`?) 확인된 근거가 없다. 대신 **체결수량 vs 주문수량**을 비교해서 filled/partially_filled를 스스로 계산한다 — 숫자는 철자와 달리 애매할 일이 없다. 상태 문자열은 취소/거부처럼 수량 비교로는 알 수 없는 두 경우에만 보조로 쓴다("cancel"/"취소", "reject"/"거부" 부분 문자열 매칭).

체결수량은 브로커가 **누적치**로 준다고 가정했다. 그래서 두 번째 조회부터는 `이번 누적 - 저장된 누적`만 새 체결로 기록한다 — 그대로 두 번 적으면 체결이 두 배로 잡힌다. 테스트가 이 델타 계산을 직접 확인한다.

### 3. 없는 API를 또 지어낼 뻔했다

`unknown` 상태 주문(주문 시 토스가 준 `orderId`를 못 받은 경우)은 `get_order(orderId)`로 조회가 안 된다. `list_orders()`로 이력을 훑어 `clientOrderId`가 일치하는 항목을 찾는 경로를 새로 만들었다 — 이건 설계 3.1이 명시적으로 "Reconciler ← GET /orders (폴링)"라고 적어둔 엔드포인트라 [8]에서 피했던 것과는 다르다. 다만 정확한 쿼리 파라미터·응답 필드명은 여전히 미확인이라, 여러 철자를 시도하는 방어적 파싱은 그대로 유지했다.

### 4. 호가단위는 일부러 안 넣었다

한국 주식 호가단위는 가격대별 계단식(2천원 미만 1원 ~ 50만원 이상 1,000원)이고, 확인된 근거 없이 규칙을 지어 넣으면 **틀린 가격을 자신 있게 보내는** 꼴이 된다. 손절 주문가를 트리거보다 살짝 낮추는 슬리피지 계산(`oco_stop_loss_slippage`, 기본 0.5%)만 하고, 원/센트 단위로만 반올림한다. 틀리면 `invalid-tick-size`로 드러난다 — 조용한 오류보다 이름 붙은 거부가 낫다.

### 5. OCO 등록 실패는 실행을 막지 않는다

`idempotency-key-conflict`(executor 쪽)와 다르게, `duplicate-conditional-order` 같은 OCO 등록 실패는 **이 종목 하나의 문제**이지 ID 생성 로직이 깨졌다는 신호가 아니다. 그래서 `ExecutorBug`처럼 올리지 않고, 실패를 기록한 뒤 다음 주문으로 넘어간다. 체결 기록 자체는 이미 저장된 뒤라 잃지 않는다.

### 6. PAPER는 reconciler를 아예 안 만든다

`Reconciler.__init__`이 LIVE가 아닌 TradingApi를 거부한다. PAPER 주문은 브로커에 전송된 적이 없어 폴링할 대상이 없다 — 가짜 체결을 시뮬레이션하는 건 백테스트 엔진(다른 세션이 이미 만든 `src/backtest/`)의 몫이지 여기 몫이 아니다.

**`--live`는 여전히 거부된다.** reconciler와 OCO는 이제 있지만 설계 [10]이 요구하는 "최소 수량 1주 실거래 검증"은 별개 단계라 메시지만 바꿨다.

**검증**: 신규 33개 포함 전체 312개 통과.

### 다음

- [10] LIVE 개방 — 최소 수량 1주부터, `--reconcile`을 스케줄러에 별도 주기로 등록
- OCO 자체의 체결/취소 상태를 확인할 조회 엔드포인트가 없음 — 확인되는 대로 추가
- 호가단위 규칙 — 실제 API 응답으로 밴드를 확인하면 `_round_to_tick`을 정확한 계단식으로 교체

---

## 2026-08-26 — Phase 2 [6] 잔여분: 모멘텀 DCA 전략 + 백테스트 러너

첫 실전 전략을 채웠다: `src.strategy.momentum_dca:MomentumDcaStrategy`. 조건은
시드가 작고(월 50~100만원 적립), 공격적이되 규칙 기반 — 상위 1~2종목 모멘텀
집중 배분, 2배 지수 ETF까지만, 추세 필터로 레버리지만 게이트.

### 1. 콜드스타트 함정

`max_position_weight` 검사는 자산이 0일 때만 건너뛰었다. 그런데 전략의
**생애 첫 매수는 정의상 비중 100%** — 시드가 작은 이번 케이스에선 엣지 케이스가
아니라 1개월차에 바로 첫 주문이 거부된다. `weight_check_min_equity_krw` 면제선과
`max_position_weight_overrides`(종목별 예외) 두 개를 게이트에 추가해서 풀었다.
전역 기본은 0.20을 그대로 두고, 집중을 허용할 종목만 config에 명시한 사실로
남긴다 — 코드가 아니라 config가 그 예외를 갖고 있어야 조용히 넓어지지 않는다.

### 2. `StrategyContext`에 필드 두 개만 추가

`history`(symbol → PriceHistory)와 `recent`(최근 신호 로그) — 둘 다 기본값과
함께 뒤에 붙여서 `risk.py`를 포함한 기존 생성 지점은 전부 그대로 돈다. 지표는
전략이 이 데이터로 직접 계산하고(`src/strategy/indicators.py`, stdlib+Decimal),
컨텍스트에 조회 가능한 콜백을 두지 않았다 — 콜백은 이름만 다른 I/O 탈출구다.

### 3. 레버리지 정책은 조건문이 아니라 `Instrument.__post_init__`

3배 이상, 인버스, 개별종목 레버리지 ETF(NVDL·TSLL류)는 유니버스에 넣는 순간
예외가 난다. config에서 읽은 값도 같은 생성자를 거치므로 정책이 코드에 있지
"조심하기로 한 약속"으로 남지 않는다.

### 4. 백테스트는 진짜 `RiskGate`를 문자 그대로 통과한다

`SimPortfolio.snapshot()`이 진짜 `PortfolioSnapshot`/`Position`을 만들기
때문에 — 목이 아니라. 리밸런스는 주 1회(월요일) 기본, 급락(−3%대 하락 +
20일 낙폭 8%+ + 쿨다운)에만 일 단위 개입. `TWR`(전략 성과)과
`IRR`(계좌 실현 성과, XIRR)을 둘 다 보고한다 — 매월 적립이 있으면 둘이 크게
갈라지고, 하나만 "CAGR"이라고 부르면 둘 중 하나는 틀린 질문에 답한 게 된다.
MDD는 원자산이 아니라 TWR 인덱스 기준 — 안 그러면 매월 입금이 낙폭을 가린다.

`python -m scripts.backtest --refresh`로 yfinance 시세를 SQLite에 캐시하고,
`--offline`이면 그 캐시만 쓴다 — 재현성이 목적이라 오프라인 모드는 구멍이
있어도 절대 네트워크를 부르지 않는다.

### 다음

- `config.yaml`에 `trading.universe`/`strategy_params`/`max_position_weight_overrides`를
  채우고 `python -m scripts.backtest --refresh --offline`로 인/아웃오브샘플
  검증 (파라미터가 ~20개라 과최적화 위험이 실재함 - 시도한 조합은 여기 기록할 것)
- PAPER로 몇 주 돌리며 같은 날짜의 백테스트 재생과 신호를 diff — 불일치는
  순수성 버그
- 개별 손절매는 이번에 파라미터만 두고 OFF — 백테스트로 넣을지 결정

## 2026-08-26 — Phase 2 [8]: PAPER 모드 주문 실행

`python trade.py`로 신호 → 리스크 게이트 → DB까지 한 번에 흐른다. **HTTP는 한 건도 나가지 않는다.**

### 1. 캘린더 파서를 공유 모듈로 — `src/toss/calendar.py`

새로 쓰지 않았다. `dashboard/service.py`에 이미 시장별로 제각각인 응답을 재귀로 훑는 파서가 있었고, 리스크 게이트가 필요로 하는 답이 정확히 같은 것이었다. 4개 함수를 `toss/calendar.py`로 올리고 대시보드는 import만 바꿨다.

새로 추가한 건 `regular_window()` 하나다. 기존 `live_session()`은 "지금 어느 세션인가"만 돌려주고 **시각을 버린다.** 게이트의 "정규장 종료 1시간 전" 규칙은 종료 시각 자체가 필요하다.

> **타임존이 조용한 지뢰였다.** `parse_dt`는 aware datetime을 준다. `ctx.now`가 naive면 `ctx.now >= cutoff`에서 `TypeError`가 난다 — 그것도 *금액 주문이 마감 1시간 안에 들어왔을 때만*. 컨텍스트 빌더가 `datetime.now().astimezone()`으로 양쪽을 맞추고, 테스트가 이 성질을 못박는다.

### 2. `client_order_id` — 시퀀스를 DB에서 세면 안 된다

처음엔 `Store.next_order_seq()`가 그날 그 전략·종목의 주문 행을 세서 +1 하도록 짰다. **테스트가 바로 잡아냈다.**

배치를 두 번 돌리면 두 번째 실행은 행이 1개 있으니 seq=2를 받는다 → **새 ID → 재발주.** 결정론적 ID가 존재하는 이유를 정확히 반대로 뒤집은 것이다.

시퀀스는 **런 내부의 순서**여야 한다. executor가 메모리에 카운터를 들고 매 실행 0에서 시작한다. 같은 배치는 같은 ID를 만들고, store가 그걸 알아본다. 이건 **전략이 순수하다는 성질에 기댄다** — 같은 컨텍스트면 같은 신호가 같은 순서로 나온다. `next_order_seq`는 지웠다.

### 3. PAPER의 이중 안전장치

`build_trading_api`가 PAPER에는 **`allow_write=False` 클라이언트**를 준다. executor의 모드 분기가 깨져 `place_order`가 호출돼도 `TossClient.request()`가 `TossWriteBlockedError`를 던진다. 안전 성질이 분기 하나의 정확성에 걸려 있지 않고 **한 층 아래, HTTP를 실제로 보내는 자리에서** 강제된다. 테스트가 이걸 직접 증명한다.

`OrderExecutor`는 자기 모드와 `TradingApi`의 모드가 다르면 생성 자체를 거부한다. 둘이 어긋날 수 있으면 그게 설계 7절이 말하는 PAPER/LIVE 혼동이다.

### 4. 에러 정책 — 거의 아무것도 재시도하지 않는다

핵심은 재시도 목록이 아니라 **재시도하지 않는다는 기본값**이다. 거부는 계좌나 시장에 대한 정보이고, 같은 요청을 반복하면 같은 거부를 레이트리밋 비용만 더 써서 받는다.

예외는 둘뿐이다.

- **`price-out-of-range`** — 밴드 안으로 당겨서 **딱 1회**. 밴드는 에러 봉투에서 먼저 찾고, 없으면 게이트가 이미 읽어둔 것을 쓴다. **둘 다 없으면 재시도하지 않는다** — 지어낸 가격으로 재전송하는 건 실패보다 나쁘다
- **`request-in-progress`** — 재전송은 포지션을 두 배로 만들 수 있으니 불가. 그런데 조회도 불가능하다: `GET /orders/{orderId}`는 토스가 부여한 ID가 필요한데 그게 바로 못 받은 것이다. 확인된 엔드포인트 목록(설계 2.2)에 clientOrderId로 찾는 경로가 없어서 **없는 API를 지어내지 않았다.** `unknown`으로 남기고 [9] reconciler에 넘긴다

`idempotency-key-conflict`는 **예외로 올려 실행을 중단**한다. 같은 ID에 다른 내용이 실렸다는 건 ID 생성이 깨졌다는 뜻이고, 그렇다면 그 뒤 모든 주문의 멱등성을 믿을 수 없다.

**모르는 에러 코드도 terminal로 취급한다.** 인식하지 못한 에러는 안전하게 재시도할 만큼 이해한 에러가 아니다.

### 5. 조회 실패는 통과가 아니라 거부 — `src/execution/context.py`

리포트 파이프라인과 **반대 방향**이다. 거기서는 조회 실패가 리포트를 degrade시킨다(`_collect_warnings`가 종목을 건너뛴다). 여기서는 실패한 필드를 **비워두고**, strict 모드가 그걸 "확인 불가"로 읽어 거부한다.

특히 주문가능금액이 None일 때 **0으로 채우지 않는다.** 0은 "현금 없음"이라는 틀렸지만 살아남는 답이고, 진짜 답은 "모름"이다.

레이트리밋: 종목별 `sellable-quantity`는 보유 종목당 1회만 돌고 컨텍스트에 캐시된다(`ORDER_INFO`는 개장 직후 3 TPS). `account_seq`도 `PortfolioService`가 이미 해석한 것을 재사용한다 — `ACCOUNT`는 1 TPS다.

### 6. `--live`는 만들었지만 열지 않았다

플래그를 받으면 배너가 아니라 **거부하고 종료한다**(exit 4). reconciler([9]) 없이는 제출한 주문이 실제로 어떻게 됐는지 알 방법이 없고, 대사되지 않은 실주문은 추적 불가능한 포지션이다. 설계 [10]이 별도 단계인 이유다.

`trading.enabled`는 기본 false. 매매 코드가 트리에 있다는 사실만으로 주문이 나가면 안 된다.

**검증**: 신규 48개 포함 전체 195개 통과. 네트워크 접근 0 — e2e 테스트가 실제로 토스 토큰 엔드포인트를 때리는 걸 한 번 잡아서 `build_trading_api`까지 스텁했다.

### 다음

- [9] reconciler — `GET /orders` 폴링으로 `fills` 채우기. `unknown` 상태 주문의 정산도 여기서
- [9] OCO 조건주문 — `Signal.stop_loss_price`/`take_profit_price`는 자리를 잡아뒀고 아직 읽지 않는다. 손절을 토스 서버에 위임하는 것이 설계가 꼽은 가장 중요한 기능
- [10] LIVE 개방 — 최소 수량 1주부터
- 전략 작성 (사용자)

---

## 2026-08-26 — Phase 2 [6][7]: 전략 레이어와 리스크 게이트

주문 코드는 아직 한 줄도 없다. 설계 6절이 `execution/risk.py`를 `toss/trading.py`보다 **먼저** 두라고 한 이유 그대로다 — 주문이 먼저 돌기 시작하면 "안전장치는 나중에"가 너무 쉬워진다. 이번 커밋 시점에서 리스크 게이트는 우회할 주문 경로 자체가 없다.

### 1. `src/strategy/base.py` — 제안, 아직 주문이 아니다

`Signal`은 전략의 *제안*이다. 잔고도, 장 운영시간도, 가격 밴드도 여기서는 보지 않는다. 그건 리스크 게이트의 일이고, 둘을 갈라놓아야 전략을 **리스크 규칙을 모른 채** 쓸 수 있다.

`Signal.__post_init__`에서 막는 것들:

| 규칙 | 이유 |
|---|---|
| `quantity` XOR `amount` | API가 둘 다 실린 body를 거부한다. 둘 중 **엉뚱한 쪽으로 조용히 체결되는 것**이 여기서 죽는 것보다 나쁘다 |
| LIMIT에 `limit_price` 필수 | 가격 없는 지정가는 주문이 될 수 없다 |
| `reason` 필수 (공백 불가) | 감사 로그가 존재하는 이유가 "왜 샀는가"다. 사후에 차트 보고 재구성한 이유는 사실이 아니라 이야기다 |

`Strategy`는 client도 store도 config도 받지 않는다. **네트워크에 닿을 수 없는 전략은 실수로 주문을 낼 수 없고, 테스트에서와 운영에서 다르게 동작할 수도 없다.** 같은 `evaluate`를 과거 컨텍스트에 그대로 흘리면 그게 백테스트다.

`StrategyContext` 하나를 전략과 리스크 게이트가 **공유**한다. 같은 순간의 같은 세계를 읽으므로 쪼개봐야 API 응답 한 벌로 객체 두 개를 만드는 일밖에 안 된다. 전략은 계좌 필드를 그냥 안 볼 뿐이다.

### 2. `src/execution/risk.py` — 유일한 `OrderIntent` 생산자

`OrderIntent`는 `RiskGate`만 만든다. 승인된 신호는 `OrderIntent`로, 거부된 신호는 **규칙 이름이 붙은 `Rejection`**으로 나온다.

게이트는 **I/O를 하지 않는다.** 주문가능금액·매도가능수량·장 운영시간·오늘 사용량은 호출자가 읽어서 컨텍스트에 실어준다. 덕분에 모든 규칙이 인자의 함수이고, 브로커를 목킹하지 않고 컨텍스트만 만들어 테스트한다. 유일한 예외인 킬 스위치 파일 확인은 자유 함수 `kill_switch_active()`로 빼뒀다.

규칙은 **싸고 절대적인 것부터** 돈다. 킬 스위치 → 일일 건수 → 소수점 수량 → 세션 → 가격 밴드 → 금액 산정 → 예산/비중 → 잔고 → 고액. 거부는 **첫 번째 규칙 하나만** 이름을 댄다 — 목록이 아니라, 고쳐야 할 그 하나다.

설계 3.3표에서 옮겨온 것들 중 판단이 필요했던 부분:

- **비중 한도는 "주문이 만들어낼 포지션"에 건다.** 주문 하나만 보면 작은 매수 열 번이 큰 포지션 하나를 조용히 쌓는다. 매도는 비중을 줄이기만 하므로 검사하지 않는다.
- **고액 주문(1억↑)의 `confirmHighValueOrder`를 자동으로 켜지 않는다.** 1억을 통과시킬 수 있는 봇은 버그 하나 거리에 있다. `allow_high_value`를 사람이 명시적으로 켠 경우에만 통과한다.
- **금액 지정·소수점 수량은 정규장 종료 1시간 전이 마감이다.** 장이 열려 있어도 거부된다(설계 2.1). 주식 수 주문은 영향 없다.
- **`RiskLimits.strict`**(기본 True) — 검증할 데이터가 없을 때의 태도. 시세도 캘린더도 주문가능금액도 없으면 **거부**한다. 확인 못 한 주문은 안전한 주문이 아니고, 422로 알게 되는 건 설계 3.3-5가 하지 말라는 바로 그것이다. 참조 데이터가 아직 안 붙은 백테스트·초기 PAPER 실행은 `strict=False`로 확인 가능한 것만 본다. 이 모드에서도 킬 스위치와 일일 한도는 그대로 산다.

`client_order_id`는 게이트가 비워둔다. 일일 시퀀스는 [8]의 executor에 있다.

### 3. 스키마 — `signals` / `rejections` / `orders` / `fills`

거부된 신호도 승인된 신호와 **같은 테이블**에 들어간다. "전략은 사고 싶어 했고 리스크 게이트가 막았다, 규칙은 이것이다"가 그날 하루를 설명하는 대부분이고, 안 적어두면 복구가 불가능하다.

`orders.notional_krw`는 비정규화해서 들고 있다. 일일 금액 한도를 `SUM` 한 번으로 끝내기 위해서다 — 재계산하려면 그날의 환율을 되찾아와야 한다.

**검증**: 신규 51개 포함 전체 147개 통과. 네트워크 접근 0.

### 다음

- [8] `toss/trading.py` + executor — PAPER 모드만. `RiskLimits`를 config에 배선하는 것도 여기서 (지금 넣으면 읽는 곳이 없는 설정이 된다)
- 결정론적 `client_order_id` 발급과 일일 시퀀스
- [9] reconciler + OCO 조건주문

---

## 2026-08-20 — 일일 자동 실행 + Gemini SDK 이관

### 1. Windows 작업 스케줄러 등록

`run_report.bat`을 그대로 등록하면 안 됐다. 마지막 줄이 `pause`라 스케줄러가 띄운 작업이 키 입력을 기다리며 영원히 끝나지 않고, 다음 날 실행은 `MultipleInstances` 규칙에 막힌다. 배치를 두 갈래로 나눴다.

| 호출 | 동작 |
|---|---|
| `run_report.bat` | 콘솔에 출력하고 `pause` — 손으로 실행할 때 |
| `run_report.bat --scheduled` | `logs\report_YYYY-MM-DD.log`에 기록하고 즉시 종료 |

배치가 `PYTHONIOENCODING=utf-8`을 지정한다. 리다이렉트된 stdout은 콘솔 코드페이지(cp949)를 따라가서, 종목명 한 글자에 로그가 통째로 깨진다. `%DATE%`도 로캘마다 형식이 달라 파일명에 못 쓰므로 PowerShell로 형식을 고정했다.

등록 설정과 근거:

| 설정 | 이유 |
|---|---|
| 매일 10:00 | 미국장 마감(서머타임 06:00 KST) 이후 + 사용 시작(09:00) 이후. 아래 `Interactive` 항목과 묶어서 읽어야 한다 |
| `StartWhenAvailable` | PC가 꺼져 있어 놓친 실행을 다음 부팅 때 따라잡는다. 스냅샷은 소급 생성이 불가능하다 |
| `MultipleInstances=IgnoreNew` | 토스는 client당 유효 토큰이 1개다. 겹쳐 돌면 서로를 로그아웃시킨다 |
| `ExecutionTimeLimit` 30분 | 외부 API가 응답을 안 할 때 무한정 매달리지 않는다 |
| `LogonType=Interactive` | 비밀번호를 저장하지 않는다. 대신 **로그온 상태에서만** 돈다 — 그래서 트리거를 사용 시간대(09:00~) 안쪽인 10:00로 잡았다. 처음엔 08:00로 뒀는데, 그 시각엔 로그온 전이라 매일 `StartWhenAvailable` 따라잡기에 의존하게 된다 |

**검증**: 스케줄러로 실제 1회 실행 → `LastTaskResult=0`, 로그 정상, 스냅샷 2행째 적재(24,573,137원, −29.05%), Notion 리포트 생성. 배치 양쪽 분기와 exit code 전파(7 → 7)는 파이썬 호출만 스텁으로 바꿔 따로 확인했다. `logs/`는 gitignore에 추가.

### 2. `google-generativeai` → `google-genai`

구 SDK는 지원이 종료돼 import할 때마다 FutureWarning을 뿜었다. 모델도 `gemini-1.5-flash`로 이미 폐기된 세대였다.

| | 이전 | 이후 |
|---|---|---|
| 패키지 | `google-generativeai` (지원 종료) | `google-genai` 2.19.0 |
| 호출 | `genai.GenerativeModel(...).generate_content(p)` | `client.models.generate_content(model=..., contents=p)` |
| 모델 | `gemini-1.5-flash` | `gemini-3.7-flash` |

**`thinking_level`을 `low`로 고정했다.** Gemini 3.x는 답하기 전에 생각하고 기본값이 `medium`이다. 세 문단짜리 일일 요약에 그만큼 쓸 이유가 없고, 매일 도는 배치라 비용·지연이 그대로 누적된다. `config.yaml`의 `google_ai.thinking_level`로 올릴 수 있고, `"off"`면 필드 자체를 안 보낸다(`minimal`은 3.7이 거부하므로 우회로 대신 미전송).

`response.text`가 `None`일 수 있다(안전성 차단 등). 그대로 반환하면 리포트에 `"None"`이 박히므로 명시적 문구로 대체했다.

Interactions API(신규 GA)로 가지 않았다. `generate_content`는 "legacy"로 분류됐을 뿐 **여전히 완전 지원**이고, 단발성 프롬프트 하나에 실행 스텝 구조를 파싱할 이유가 없다. 신규 모델·기능이 Interactions에 먼저 붙으므로 Phase 2에서 재검토한다.

`scripts/smoke_test.py`에 **7) Gemini** 단계를 추가했다. 토큰 한 개짜리 질문을 실제로 던진다 — 키가 틀리거나 모델명이 폐기되면 리포트 안에 사과 한 줄로만 남아 놓치기 쉽다.

**검증**: 테스트 83개 통과, `google.generativeai`가 `sys.modules`에 없음을 단언, 스케줄 실행 1회 정상 완료.

### 3. `GOOGLE_AI_API_KEY` 입력 + 그 과정에서 잡은 버그 2개

키를 채우고 `scripts/smoke_test.py` 7번 단계 통과(`generate_content: OK`). 실제 포트폴리오·뉴스로 드라이런(저장 없음)까지 돌려 보니 AI 섹션이 5.2초/약 1,000자로 생성된다. 그런데 그 출력에서 두 가지가 드러났다.

**① 종목명이 티커로만 나가서 AI가 다른 회사를 분석했다.**

`/api/v1/stocks`는 미국 종목의 `name`에 티커를 그대로 돌려주고, 실제 이름은 `englishName`에 담는다(한국 종목만 `name`에 한글명이 있다). 코드가 `name`만 읽고 `englishName`을 버려서 프롬프트에 `IONX`가 맨 티커로 들어갔다.

그 결과 첫 드라이런에서 모델이 IONX를 **IonQ 보통주**로 읽고 "지지선 확인 후 반등 시 일부 현금화" 같은, 보통주에나 맞는 조언을 냈다. 실제로는 `DEFIANCE DAILY TARGET 2X LONG IONQ ETF` — **2배 레버리지 ETF**다. 같은 답변에서 TSLL은 티커가 널리 알려진 덕에 음의 복리 효과를 정확히 지적했다. 즉 모델이 아는 티커냐 아니냐에 따라 조언 품질이 갈리고 있었다.

`src/models.py`에 `display_name()`을 두고 두 소스가 함께 쓰게 했다. `name`이 비었거나 심볼과 같으면 `englishName`으로 내려간다 — 한글명이 있으면 그대로 이긴다. 수정 후 재실행하니 모델이 두 종목 모두를 2배 레버리지로 인식하고 양쪽에 변동성 잠식을 경고한다.

> 포트폴리오가 우연히 전부 미국 ETF라 전 종목이 이 경로였다. 대시보드·Notion 리포트의 표시 이름도 같이 고쳐진다.

**② SDK가 매 실행 로그에 쓰지도 않는 기능의 경고를 남겼다.**

`google-genai`는 tools를 안 넘겨도 자동 함수 호출(AFC)을 기본 활성화하고, `Models.generate_content` 직접 호출에 대해 "AFC는 Chat.send_message를 쓰라"는 경고를 로거에 남긴다. 이 프로젝트는 산문 한 덩어리를 받을 뿐 도구를 선언하지 않으므로 `automatic_function_calling.disable=True`로 껐다. 스케줄 실행 로그에 매일 쌓일 잡음이었다.

`_config()`가 이제 항상 config를 반환한다(thinking이 꺼져 있어도 AFC 설정은 실려야 한다).

**검증**: 테스트 86개 통과(신규 3개 — `englishName` 폴백, 한글명 우선, 심볼 폴백).

### 4. 상품 유형을 프롬프트에 실었다

①의 수정은 이름이 길고 설명적이라 통했을 뿐이다(`... 2X LONG IONQ ETF`). 이름이 짧거나 애매한 상품이 들어오면 모델은 다시 티커로 추측한다. 이름에 기대는 대신 **토스가 이미 내려주는 메타데이터**를 그대로 넘기기로 했다.

`/api/v1/stocks` 응답에 `securityType`("ETF")과 `leverageFactor`("2")가 들어 있는데 둘 다 버려지고 있었다. `Position`에 두 필드를 추가하고 `instrument` 프로퍼티로 렌더한다 — 배수가 음수면 인버스로 읽는다. DB 스키마는 건드리지 않았다(정적 참조 데이터이고, `position_snapshots`는 컬럼을 명시적으로 나열해 쓴다).

프롬프트의 보유 종목 줄이 이렇게 바뀐다:

```
- DEFIANCE DAILY TARGET 2X LONG IONQ ETF [IONX] - 2x leveraged (daily reset) ETF, US: 325 shares, Profit: -22.77%, ...
```

여기에 지시를 하나 붙였다: **주석을 신뢰하고 티커나 이름으로 상품을 추측하지 말 것, 레버리지·인버스 ETF를 기초자산 주식처럼 분석하지 말 것.**

재실행하니 모델이 "2배 레버리지 일일 리셋 ETF"로 먼저 규정하고 양쪽에 변동성 잠식을 설명한 뒤, 기초자산(IonQ·Tesla)은 기초자산으로만 언급한다. ①이 이름 덕에 우연히 맞힌 것을, 이번 변경은 데이터로 보장한다.

**검증**: 테스트 88개 통과(신규 2개 — 레버리지 ETF 라벨링, 인버스·무레버리지 렌더링).

### 참고

구 `google-generativeai` 패키지는 Windows Python에서 **제거하지 않았다**. 이제 아무도 import하지 않아 경고가 뜨지 않고, 같은 인터프리터를 쓰는 다른 프로젝트를 깨뜨릴 이유가 없다.

---

## 2026-08-19 — Phase 1: 토스 Open API 전환 + SQLite + 대시보드

### 배경

기존 v1은 보유 수량·평단을 전부 `config.yaml`에 수기 입력하고 yfinance로 시세만 가져오는 반자동 방식이었다. 산출물은 Notion 리포트 한 장뿐이고 저장소가 없어 "어제 대비"조차 계산할 수 없었다.

토스증권 Open API가 국내(KRX)+미국 주식을 단일 REST API로 제공하면서, 키움 API를 폐기했던 세 가지 이유(①국내/해외 API 파편화 ②32비트 Python 강제 ③해외 잔고 조회 부재)가 모두 해소되었다.

### 결정 사항

| 결정 | 근거 |
|---|---|
| **yfinance 완전 제거** | 수기 종목의 시세도 토스 `/prices`로 커버되므로 데이터 소스를 둘로 유지할 이유가 없음 |
| **손익을 재계산하지 않고 API 값 사용** | `/holdings`가 수수료·세금 차감 손익(`amountAfterCost`)까지 계산해 내려줌. 재계산하면 같은 질문에 답이 두 개 생김 |
| **전 구간 `Decimal`** | 토스가 모든 금액을 문자열로 반환. float은 원 단위 오차가 누적됨 |
| **토큰 캐시 + 파일 락** | 토스는 client당 유효 토큰이 1개이고 재발급 시 이전 토큰을 즉시 무효화. 캐시는 성능이 아니라 정합성 요구사항 |
| **`allow_write=False` 기본값** | 리포팅 단계에서 주문 엔드포인트가 닿을 수 없게 구조적으로 차단 |
| **SQLite 적재를 Phase 1에 포함** | 자산 추이 차트는 과거를 소급 생성할 수 없음. 적재 시작이 늦을수록 손해 |
| **대시보드 서버측 캐시** | `ACCOUNT` 그룹이 1 TPS. 브라우저 탭마다 API를 호출하면 즉시 429 |

### 구현

```
src/
├── config.py            # .env 우선 자격증명, 심볼 정규화(.KS 제거), v1 하위호환
├── toss/
│   ├── client.py        # 토큰 캐시·레이트리밋·429/5xx 백오프·쓰기 가드
│   ├── account.py       # 계좌 · 보유주식 · 매수가능금액
│   ├── market.py        # 시세 · 종목마스터 · 환율 · 캘린더 · 매수유의사항
│   ├── ratelimit.py     # 그룹별 토큰버킷 (X-RateLimit-Limit 관측 반영)
│   ├── errors.py        # 에러코드 → 예외 타입, TERMINAL_CODES
│   └── _filelock.py     # msvcrt/fcntl 크로스 플랫폼 락
├── sources/             # toss_source(자동) · manual_source(config+시세조회)
├── portfolio.py         # 소스 병합 · 중복 심볼 가중평균 · KRW 환산
├── models.py            # Position · PortfolioSnapshot
├── store/               # SQLite (snapshots · position_snapshots · reports)
├── pipeline.py          # 클라이언트~집계 배선 (main.py와 대시보드가 공유)
└── dashboard/           # FastAPI + 정적 프론트엔드
```

**변경**: `main.py`(배선 교체 + exit code), `notion.py`(전일대비·실질수익률·환차손익 배지), `analyst.py`(프롬프트 입력 구조)
**삭제**: `portfolio_manager.py`, `requirements.txt`의 `yfinance`
**신규 문서**: `DESIGN_Toss_API_Migration.md`, `DESIGN_Trading_and_Dashboard.md`, `config.example.yaml`, `.env.example`, `docs/ui/`

### 새로 가능해진 것

- **전일 대비 손익** (`dailyProfitLoss`) — v1에서는 이력 DB 없이 불가능했던 지표
- **수수료·세금 차감 후 실질 수익률** (`rateAfterCost`)
- **매수 유의사항 경보** — 정리매매·투자경고·VI 발동을 리포트 상단과 대시보드 배너에 표시
- **자산 추이 시계열** — SQLite 적재 시작
- **실패 감지** — 리포트 실패 시 exit code 2/3 반환 (기존엔 Notion이 실패해도 0)

### 남은 수기 입력

토스 계좌 보유분은 완전 자동. 타 증권사 보유분만 `(수량, 평단)` 두 값이 남는다 — 종목명·현재가·통화는 API가 채운다.

예외는 **매수 시점 환율**. 토스도 제공하지 않으므로(`averagePurchasePrice`는 원화폐 기준) 환차손익을 수익률에 반영하려면 `avg_exchange_rate`를 직접 입력해야 한다. 미입력 시 "환차손익 미반영" 배지를 표시해 수치를 넓게 해석하지 않도록 했다.

### 검증

- **테스트 63개** — 네트워크·자격증명 없이 전부 통과 (HTTP 모킹)
- **환산 정확도** — AAPL 10주 × $178.5 × 1,342.5 = 2,396,362.5원, 매입은 매수환율 1,300원 적용
- **캐시 동작** — 탭 5개가 10회 호출해도 상위 `/holdings` 호출은 1회
- **팔레트** — 도넛 색상(`#00a572`/`#2563eb`)을 CVD 검증기로 확인, 6개 검사 통과(ΔE 26.5)
- **실계좌 스모크 테스트** — 계좌 조회 OK, 허용 IP 통과, USD/KRW 1,397.10 수신
- **토큰 캐시 실증** — 2회차 실행 시 발급 0회, 캐시 파일 mtime 불변, 유효기간 약 24시간

### 구현 중 잡은 버그

1. **범례 색상이 라벨 전체에 칠해짐** — `querySelector("div div")`가 문서 기준으로 매칭돼 스와치 대신 flex 컨테이너를 선택. 클래스 기반 선택자로 교체
2. **수기 종목 P&L이 빈칸** — 손익을 토스 소스만 채우고 있었음. `ManualSource`에서 네이티브 통화 기준으로 계산 추가
3. **해시 딥링크 무반응** — 해시 변경은 same-document navigation이라 `hashchange` 리스너 추가
4. **플레이스홀더 자격증명이 401로 나타남** — `.env`를 안 채우면 API가 401을 반환해 "키가 틀렸나?"로 헤매게 됨. `PASTE_` 접두사를 감지해 어느 키가 미입력인지 알려주도록 수정

### 운영상 주의점

- **허용 IP 등록 필수** — 미등록 IP는 403 `edge-blocked`로 전량 차단. 동적 IP 환경에서는 어느 날 갑자기 실패한다
- **토큰 1개 제약** — 같은 자격증명으로 두 프로세스를 동시에 돌리면 서로를 로그아웃시킨다. 파일 락으로 방어했지만 별도 머신에서 동시에 돌리면 여전히 충돌
- **대시보드는 `127.0.0.1` 바인딩 유지** — 인증이 없고 Phase 2 이후 실계좌 제어 화면이 된다

### 첫 실행 (16:01, exit 0)

`config.yaml` 작성 → `python main.py` 실행. 파이프라인이 끝에서 끝까지 동작했다.

```
Summary: 24,182,741 KRW (P&L -10,452,454, -30.18%)
Snapshot saved (2026-08-19T16:01:01, total 1 rows)
Fetched 5 general news items.
[Notion] Successfully created report
```

| 단계 | 결과 |
|---|---|
| 포트폴리오 조회 | 24,182,741원 (−30.18%), USD/KRW 1,398.4 |
| SQLite 적재 | `snapshots` 1행 · `position_snapshots` 2행 — 자산 추이 기록 시작 |
| 뉴스 | 5건 |
| Notion 리포트 | 생성 완료 (StockLens AI 페이지 하위) |

두 가지가 예상과 달랐다.

- **토스 계좌 보유분이 0건**이었다. 적재된 2행(IONX·TSLL)은 전부 `source=manual`이고, 총액도 수기 종목 시가(17,293.15 USD × 1,398.4)와 원 단위까지 일치한다. 자동 조회 경로 자체는 계좌·시세·환율 응답으로 검증됐지만, **토스 보유분이 실린 스냅샷은 아직 한 번도 없다** — 실제로 매수하기 전까지 그 경로는 미검증으로 둔다.
- **AI 분석은 건너뛰었다.** `GOOGLE_AI_API_KEY`가 플레이스홀더라 리포트에 `AI Analyst is not configured (Missing API Key).`가 그대로 들어갔다. 리포트 생성 자체는 정상.

`daily_profit_krw`는 0이다. 전일 스냅샷이 없어서가 아니라 토스 보유분이 없어 `dailyProfitLoss`를 받을 곳이 없기 때문이다.

### 다음

- **일일 자동 실행 등록** — Windows 작업 스케줄러에 `run_report.bat`. 스냅샷이 매일 쌓여야 Portfolio Value 차트에 선이 그려진다(점 3개부터). Phase 1의 남은 값은 대부분 여기서 나온다
- **`GOOGLE_AI_API_KEY` 입력** — 넣으면 다음 실행부터 AI 섹션이 붙는다
- Phase 2: `src/toss/trading.py`부터. 구현 순서는 **리스크 게이트 → 주문 실행 → 조건주문(OCO 손절)** — 주문 코드가 먼저 동작하면 안전장치가 뒤로 밀린다
- 별건: `google-generativeai` SDK가 deprecated 상태이고(실행마다 경고 출력) 기본 모델도 구형(`gemini-1.5-flash`). 토스 마이그레이션과 무관하므로 분리해서 진행
